---
phase: 04-stadtstaaten-plz-wahlkreis-genauigkeit-und-wahlkreis-gruppie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/data/raw/plz-polygons-stadtstaaten.geojson
  - web/package.json
  - web/package-lock.json
  - web/scripts/filter-plz-polygons.ts
  - web/src/app/(site)/datenschutz/page.tsx
autonomous: true
requirements: [PRIV-02]

must_haves:
  truths:
    - "The PLZ-area polygon dataset for Berlin/Hamburg/Bremen exists in the repo as a small filtered file (not the full 8176-feature file)"
    - "The build script has the geometry libraries it needs (@turf/intersect, @turf/area) as build-only devDependencies"
    - "The public site carries the required ODbL attribution for the OpenStreetMap-derived PLZ geodata"
    - "PLZ-to-Wahlkreis resolution remains static/offline (no runtime API call) — the new dataset is build-time input only"
  artifacts:
    - path: "web/data/raw/plz-polygons-stadtstaaten.geojson"
      provides: "PLZ-area polygons for the three Stadtstaaten (Berlin/Hamburg/Bremen)"
      contains: "\"postcode\""
    - path: "web/package.json"
      provides: "turf devDependencies declared"
      contains: "@turf/intersect"
    - path: "web/src/app/(site)/datenschutz/page.tsx"
      provides: "ODbL attribution for PLZ geodata"
      contains: "OpenStreetMap"
  key_links:
    - from: "web/data/raw/plz-polygons-stadtstaaten.geojson"
      to: "web/scripts/parse-plz-mapping.ts"
      via: "build-time file read in plan 04-02"
      pattern: "plz-polygons-stadtstaaten"
---

<objective>
Lay the data + dependency + legal foundation for the Stadtstaat PLZ→Wahlkreis precision fix.

Purpose: The actual fix (plan 04-02) needs three inputs that do not yet exist in the repo: a PLZ-area polygon dataset filtered to the three Stadtstaaten, the turf.js geometry libraries to compute polygon intersection, and the ODbL attribution that the OpenStreetMap-derived data legally requires. All three are APPROVED (Thomas signed off 2026-05-27). This plan acquires/installs them. No build-script logic changes here — that is plan 04-02.

Output:
- `web/data/raw/plz-polygons-stadtstaaten.geojson` (~1.78 MB, 324 features, Berlin/Hamburg/Bremen only) committed to the repo.
- `@turf/intersect` + `@turf/area` (7.3.5) added as devDependencies.
- One-line ODbL attribution added to the Datenschutz page (Section 5, where PLZ data sources are already described).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stadtstaaten-plz-wahlkreis-genauigkeit-und-wahlkreis-gruppie/04-CONTEXT.md
@.planning/phases/04-stadtstaaten-plz-wahlkreis-genauigkeit-und-wahlkreis-gruppie/04-RESEARCH.md

<dataset_facts>
Source: yetzt/postleitzahlen, release 2026.02 (ODbL-1.0, OSM-derived).
Brotli-compressed release asset:
https://github.com/yetzt/postleitzahlen/releases/download/2026.02/postleitzahlen.geojson.br

Verified file shape (do NOT re-derive — use these facts):
- Top level: { "copyright": "© OpenStreetMap contributors...", "license": "ODbL-1.0", "type": "FeatureCollection", "features": [...] }
- Each feature: { "type": "Feature", "id": "relation/...", "properties": { "postcode": "20249", "rel": "..." }, "geometry": { "type": "Polygon" | "MultiPolygon", "coordinates": [...] } }
- PLZ property name is "postcode" (5-digit string).
- Coordinate order is GeoJSON standard [longitude, latitude].
- Full Germany: 8176 features. Stadtstaat subset: 324 features (Berlin 191, Hamburg 100, Bremen 33; 17 are MultiPolygon).
- Full uncompressed ~502 MB — DO NOT commit. Filtered subset ~1.78 MB — DO commit.

Stadtstaat PLZ ranges to keep (must match the ranges already in parse-plz-mapping.ts):
- Berlin: 10000–14199
- Hamburg: 20000–21149 OR 22001–22799
- Bremen: 28100–28779
</dataset_facts>

<attribution_facts>
Datenschutz page Section 5 ("5. Verarbeitung der Postleitzahl", web/src/app/(site)/datenschutz/page.tsx ~lines 152–166) already describes the PLZ→Wahlkreis data as "statische Referenzdaten der Bundeswahlleiterin". This is where the ODbL line belongs.

Required attribution text (German, user-facing, no em dash — use hyphen):
"Postleitzahl-Geodaten: © OpenStreetMap contributors, ODbL 1.0"
</attribution_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire + filter the PLZ polygon dataset to the Stadtstaat subset and commit it</name>
  <read_first>
    - .planning/phases/04-stadtstaaten-plz-wahlkreis-genauigkeit-und-wahlkreis-gruppie/04-RESEARCH.md (Research Question 1 + "File to commit to repo")
    - web/scripts/parse-plz-mapping.ts (lines 57–76 for the exact STADTSTAAT_PLZ_RANGES test predicates to mirror)
    - web/data/PLZ-MAPPING-STRATEGY.md (the "Files in this strategy" table — the new file slots here)
  </read_first>
  <action>
    Write a one-time filter script at `web/scripts/filter-plz-polygons.ts` that:
    1. Reads the full yetzt/postleitzahlen GeoJSON. Fetch the release asset
       https://github.com/yetzt/postleitzahlen/releases/download/2026.02/postleitzahlen.geojson.br
       and decompress it with Node's built-in `zlib.brotliDecompressSync` (no new runtime dep — zlib is in Node core). Do this fetch+decompress inside the script (accept a local path arg as an override so the script can be re-run offline if the .br is already downloaded to /tmp).
    2. Filters `features` to keep ONLY features whose `properties.postcode` is a 5-digit string that falls in a Stadtstaat range:
       - Berlin: n >= 10000 && n <= 14199
       - Hamburg: (n >= 20000 && n <= 21149) || (n >= 22001 && n <= 22799)
       - Bremen: n >= 28100 && n <= 28779
       (These predicates MUST match STADTSTAAT_PLZ_RANGES in parse-plz-mapping.ts exactly.)
    3. Writes a FeatureCollection preserving the top-level `copyright` and `license` fields plus the filtered `features` to `web/data/raw/plz-polygons-stadtstaaten.geojson`, pretty-printed.
    4. Logs the feature count and a per-Stadtstaat breakdown.

    Run the script (`npx tsx scripts/filter-plz-polygons.ts` from web/). Expected output: 324 features total. If the upstream release shifted by a few features, accept anything in the 300–340 range and log the actual count — do NOT hard-fail on an off-by-N, but DO fail loud if the result is < 250 features (that signals a filter or property-name bug). Commit the resulting `web/data/raw/plz-polygons-stadtstaaten.geojson` to the repo. Do NOT commit the full 8176-feature file or the .br download.

    Keep `filter-plz-polygons.ts` committed too (it documents how the subset was produced and lets a future refresh be reproducible). Do NOT wire it into `npm run build:plz` — it is a one-time/occasional refresh tool, not part of the regular build.
  </action>
  <verify>
    <automated>cd web && test -f data/raw/plz-polygons-stadtstaaten.geojson && node -e "const g=require('./data/raw/plz-polygons-stadtstaaten.geojson'); const n=g.features.length; const codes=g.features.map(f=>f.properties.postcode); if(n<250)throw new Error('too few features: '+n); if(!codes.includes('20249'))throw new Error('missing 20249'); if(!codes.includes('20354'))throw new Error('missing 20354'); if(!codes.includes('10997'))throw new Error('missing 10997'); console.log('OK features='+n);"</automated>
  </verify>
  <acceptance_criteria>
    - File `web/data/raw/plz-polygons-stadtstaaten.geojson` exists and parses as JSON with a top-level `features` array.
    - `features.length` is between 250 and 340 (target 324).
    - Every feature has `properties.postcode` as a 5-digit string and a `geometry.type` of either "Polygon" or "MultiPolygon".
    - PLZs 20249, 20354, 22417, 10997, 28195 are all present in the file (grep `"postcode": "20249"` etc. succeeds).
    - The full 8176-feature file is NOT committed (no file larger than ~3 MB added under web/data/raw/).
    - `web/scripts/filter-plz-polygons.ts` exists and is committed.
  </acceptance_criteria>
  <done>The filtered Stadtstaat PLZ-polygon GeoJSON (~324 features) is committed; the producer script is committed; the full file is not committed.</done>
</task>

<task type="auto">
  <name>Task 2: Add @turf/intersect + @turf/area as build-only devDependencies</name>
  <read_first>
    - web/package.json (current devDependencies block — add alongside tsx/jest)
    - .planning/phases/04-stadtstaaten-plz-wahlkreis-genauigkeit-und-wahlkreis-gruppie/04-RESEARCH.md (Research Question 2)
  </read_first>
  <action>
    From `web/`, run: `npm install --save-dev @turf/intersect@7.3.5 @turf/area@7.3.5`.
    This pulls `polyclip-ts` (the clipping engine) transitively. These are devDependencies ONLY — they run in the build script (`tsx scripts/parse-plz-mapping.ts`), never at runtime. Do NOT add them to `dependencies`. Per project memory (no-unsolicited-tools), these two packages are already APPROVED by Thomas (2026-05-27) — install them, do not re-ask.
    Verify the install with a smoke import so plan 04-02 can rely on the API shape:
    `npx tsx -e "import {intersect} from '@turf/intersect'; import {area} from '@turf/area'; console.log(typeof intersect, typeof area)"` should print `function function`.
  </action>
  <verify>
    <automated>cd web && node -e "const p=require('./package.json'); if(!p.devDependencies['@turf/intersect'])throw new Error('missing @turf/intersect'); if(!p.devDependencies['@turf/area'])throw new Error('missing @turf/area'); if(p.dependencies&&(p.dependencies['@turf/intersect']||p.dependencies['@turf/area']))throw new Error('turf must be devDependency only'); console.log('OK turf devDeps');" && npx tsx -e "import {intersect} from '@turf/intersect'; import {area} from '@turf/area'; if(typeof intersect!=='function'||typeof area!=='function')throw new Error('bad turf API'); console.log('OK turf import');"</automated>
  </verify>
  <acceptance_criteria>
    - `web/package.json` devDependencies contains `"@turf/intersect"` and `"@turf/area"`.
    - Neither `@turf/intersect` nor `@turf/area` appears under `dependencies`.
    - `web/package-lock.json` reflects the install (turf entries present).
    - `npx tsx -e "import {intersect} from '@turf/intersect'; ..."` runs without error and prints `function function`.
  </acceptance_criteria>
  <done>Both turf packages are installed as devDependencies, importable from a tsx script, and absent from runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Add the ODbL attribution for PLZ geodata to the Datenschutz page</name>
  <read_first>
    - web/src/app/(site)/datenschutz/page.tsx (Section 5 "Verarbeitung der Postleitzahl", ~lines 152–166)
    - $HOME/.claude/get-shit-done/references/... not needed; instead apply project memory: no-em-dashes + avoid-ai-writing
  </read_first>
  <action>
    In `web/src/app/(site)/datenschutz/page.tsx`, Section 5 ("5. Verarbeitung der Postleitzahl"), append one short sentence to the existing paragraph (or add a one-line note directly after it) stating the source/license of the new PLZ-area geodata. Exact German copy (no em dash, use hyphen/comma):

    "Postleitzahl-Geodaten: © OpenStreetMap contributors, ODbL 1.0."

    Keep it factual and plain — do not embellish. This is a backend/legal copy line, NOT a redesign of the page, so this task does NOT need /frontend-design or /taste (it is a one-line text insertion into an existing static legal page, no layout or component work). The link target for OpenStreetMap copyright (https://osm.org/copyright) may be added as a plain anchor if it matches the page's existing link style, but it is optional — the text line is the legal minimum.

    Run the existing AI-writing checklist mentally on the one line (it is a license attribution, so it is inherently non-fluffy — confirm no em dash, no marketing tone).
  </action>
  <verify>
    <automated>cd web && grep -q "OpenStreetMap" "src/app/(site)/datenschutz/page.tsx" && grep -q "ODbL" "src/app/(site)/datenschutz/page.tsx" && ! grep -q "—" "src/app/(site)/datenschutz/page.tsx" && echo "OK attribution present, no em dash"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/app/(site)/datenschutz/page.tsx` contains the strings "OpenStreetMap" and "ODbL".
    - The attribution text uses a hyphen in "Postleitzahl-Geodaten" and contains NO em dash (—) anywhere in the file.
    - The attribution sits inside Section 5 (Postleitzahl), not in an unrelated section.
    - `npm run lint` (or `next lint`) passes with the edit in place.
  </acceptance_criteria>
  <done>The Datenschutz page carries the one-line ODbL/OpenStreetMap attribution for the PLZ geodata, in German, with no em dash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GeoJSON release → repo (build/dev machine) | Third-party data file pulled once, decompressed and filtered locally; becomes a committed static artifact. No runtime path. |
| committed legal copy → public site | Static text rendered server-side; no user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | yetzt/postleitzahlen .br download | accept | File is fetched once from a pinned GitHub release tag (2026.02), inspected by the filter script (feature count + property-name assertions), and committed/reviewed in git before any use. Low value, vetted source (OSM-derived). No runtime fetch. |
| T-04-02 | Information disclosure | plz-polygons-stadtstaaten.geojson content | accept | Contains only public PLZ-area boundaries (OSM data). No PII. ODbL attribution added (T-04 legal, see Task 3) to satisfy the license, tracked as a task not a vuln. |
| T-04-03 | Denial of service | brotli decompress of ~502 MB file at build time | accept | Runs on the dev/build machine only (never in a request path), one-time, with the full file discarded after filtering. Memory cost is bounded and offline. |
</threat_model>

<verification>
- `web/data/raw/plz-polygons-stadtstaaten.geojson` parses and contains ~324 PLZ features including 20249/20354/22417/10997/28195.
- `web/package.json` declares both turf packages as devDependencies only; they import cleanly under tsx.
- Datenschutz page shows the ODbL attribution with no em dash; lint passes.
- No runtime API call introduced (PRIV-02 preserved): the new data is a committed static file consumed only by the build script.
</verification>

<success_criteria>
- Filtered Stadtstaat PLZ-polygon dataset committed (250–340 features).
- turf devDependencies installed and importable, absent from runtime deps.
- ODbL attribution live on the Datenschutz page (German, hyphen not em dash).
</success_criteria>

<output>
After completion, create `.planning/phases/04-stadtstaaten-plz-wahlkreis-genauigkeit-und-wahlkreis-gruppie/04-01-SUMMARY.md`
</output>
