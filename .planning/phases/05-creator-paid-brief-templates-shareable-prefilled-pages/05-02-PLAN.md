---
phase: 05-creator-paid-brief-templates-shareable-prefilled-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - web/src/app/(site)/kampagne/starten/page.tsx
  - web/src/components/campaigns/CreatorCampaignForm.tsx
  - web/src/lib/actions/createCampaignDraft.ts
  - web/src/lib/actions/verifyCampaignEmail.ts
  - web/src/lib/email/sendCampaignCreatorEmail.ts
  - web/src/lib/email/buildCampaignCreatorEmailHtml.ts
autonomous: false
requirements:
  - ENGM-03
  - SAFE-03
  - PRIV-01
---

<objective>
Build the creator-facing publish pipeline: campaign form, moderation boundary, email verification, activation, and creator emails.

This plan is the first validation slice. It must prove that Supabase-backed campaign creation and no-login creator access work before any Stripe/payment work is added.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-creator-paid-brief-templates-shareable-prefilled-pages/05-CONTEXT.md
@.planning/phases/05-creator-paid-brief-templates-shareable-prefilled-pages/05-RESEARCH.md
@web/src/lib/moderation/moderateText.ts
@web/src/lib/email/sendLetterEmail.ts
@web/src/lib/email/buildEmailHtml.ts
@web/src/lib/campaigns/repository.ts
@web/src/lib/campaigns/tokens.ts

<must_haves>
- Creator flow stays no-login.
- Campaign text is moderated before any public activation.
- Stripe/payment is not part of this plan.
- Creator receives clear emails for activation and later management.
</must_haves>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the creator setup form and draft creation action</name>
  <action>
    Add the public entry route and form for campaign creation.

    The form must collect:

    - creator email
    - campaign title
    - prefilled issue text
    - optional creator or NGO name
    - optional short description
    - optional external link
    - chosen slug

    The server action must:

    - validate and normalize all fields
    - moderate `issue_text` and `description`
    - reserve the slug by creating a draft campaign row
    - reject blocked content with a clear German error
    - create a `verify_email` token and send the creator verification email
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && grep -RIn "creator email\|slug\|moderateText\|verify_email" src</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add email verification and campaign activation</name>
  <action>
    Implement the verification route or action that:

    - consumes a `verify_email` token
    - marks the creator email as verified
    - activates the campaign only if the latest public text still passes moderation
    - creates or rotates a management token
    - sends the creator a management email with the campaign URL and edit/pause instructions

    The action must be idempotent. Reopening a used verification link should show a useful status instead of duplicating emails or corrupting campaign state.
  </action>
  <verify>
    <automated>cd web && grep -RIn "awaiting_email_verification\|email_verified_at\|active\|management" src/app src/lib</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add creator email delivery and post-activation copy</name>
  <action>
    Implement the creator email module and HTML builder. The email copy should prioritize:

    - campaign URL
    - activation status
    - edit/pause instructions
    - no-login explanation
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && grep -RIn "verify_email\|active\|management" src/lib/email src/lib/actions src/app</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the no-payment activation flow end to end</name>
  <how_to_verify>
    1. Create a draft campaign with a test email and safe content.
    2. Confirm the campaign is not public before email verification.
    3. Open the verification email.
    4. Confirm the campaign becomes public only after verification.
    5. Confirm the management email arrives with a working creator access link.
  </how_to_verify>
  <resume_signal>Type "approved" or describe the failure point.</resume_signal>
</task>

</tasks>

<verification>
- Creator form validates and creates a moderated draft.
- Email verification transitions the draft correctly and idempotently.
- Activation requires email verification.
- Creator receives working transactional emails through Brevo.
</verification>

<success_criteria>
- A creator can create and activate a campaign without an account or payment setup.
- Unsafe campaign text is blocked before public go-live.
- The activation path is recoverable by email, not by manual admin work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-creator-paid-brief-templates-shareable-prefilled-pages/05-02-SUMMARY.md`
</output>
