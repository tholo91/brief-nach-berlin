---
phase: 05-creator-paid-brief-templates-shareable-prefilled-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - web/src/app/(site)/kampagne/[slug]/page.tsx
  - web/src/components/campaigns/CampaignHero.tsx
  - web/src/components/campaigns/CampaignIssueStarter.tsx
  - web/src/lib/wizard-handoff.ts
  - web/src/lib/landing-draft.ts
  - web/src/components/wizard/WizardShell.tsx
autonomous: false
requirements:
  - ENGM-03
  - PRIV-01
  - SAFE-03
---

<objective>
Build the public campaign landing page that feels like Brief nach Berlin, keeps the issue editable, and reuses the existing wizard instead of creating a second letter flow.

This plan owns the visitor experience. The campaign page should feel specific and trustworthy, but still clearly belong to the existing product.
</objective>

<context>
@.planning/phases/05-creator-paid-brief-templates-shareable-prefilled-pages/05-CONTEXT.md
@.planning/phases/05-creator-paid-brief-templates-shareable-prefilled-pages/05-RESEARCH.md
@web/src/app/page.tsx
@web/src/components/Hero.tsx
@web/src/components/wizard/Step2Issue.tsx
@web/src/components/wizard/WizardShell.tsx
@web/src/lib/wizard-handoff.ts
@web/src/lib/landing-draft.ts

<must_haves>
- Public route serves only active campaigns.
- Prefilled issue is editable, not locked.
- CTA continues into the existing wizard.
- Visitor-edited issue text does not persist in URLs or server storage.
- Visible disclaimer states that Brief nach Berlin does not endorse creator-authored text.
</must_haves>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the public campaign route and fetch contract</name>
  <action>
    Add `/kampagne/[slug]` as a server-rendered public route that:

    - loads only active campaigns
    - returns 404 or inactive-state handling for paused/archived/nonexistent campaigns
    - exposes public fields only
    - renders optional creator attribution and external link when present
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && rg -n "kampagne/\\[slug\\]|active campaigns|notFound" src/app src/lib</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build the landing-like campaign UI with editable issue starter</name>
  <action>
    Implement the campaign page UI with the existing brand language, not a generic dashboard look.

    Required visible elements:

    - campaign title
    - optional short description
    - optional creator/NGO attribution
    - prefilled editable issue field
    - distinct CTA below the field
    - visible non-liability disclaimer

    The page must stay letter-first. It is not a browse directory or petition portal.
  </action>
  <verify>
    <automated>cd web && rg -n "Haftung|Hinweis|CTA|Step2Issue" src/components/campaigns src/app/(site)/kampagne</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Reuse wizard handoff safely for campaign visitors</name>
  <action>
    Extend the landing-to-wizard handoff and per-tab draft behavior so campaign pages can pass the issue into `/app` without colliding with the generic landing draft.

    Requirements:

    - no `?text=` URL param for campaign issue text
    - campaign draft persistence is namespaced by slug
    - wizard can consume the campaign handoff exactly once
    - visitor can still edit the issue again inside the wizard
  </action>
  <verify>
    <automated>cd web && rg -n "campaign|slug|handoff|sessionStorage" src/lib/wizard-handoff.ts src/lib/landing-draft.ts src/components/wizard/WizardShell.tsx && npx tsc --noEmit</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the public campaign flow in browser</name>
  <how_to_verify>
    1. Open an active campaign URL.
    2. Confirm the page feels like a campaign-specific Brief nach Berlin landing page, not a bare form.
    3. Edit the prefilled text.
    4. Continue into `/app`.
    5. Confirm the wizard opens with the edited text and no text is present in the URL.
    6. Navigate back and confirm the per-tab campaign draft survives as expected.
  </how_to_verify>
  <resume_signal>Type "approved" or describe what breaks.</resume_signal>
</task>

</tasks>

<verification>
- `/kampagne/[slug]` serves active campaigns only.
- Editable prefill hands off into the existing wizard.
- Campaign visitor text never lands in the URL.
- Draft persistence is isolated by campaign slug.
</verification>

<success_criteria>
- Campaign visitors immediately understand what the campaign is and can act on it.
- The existing letter-generation flow remains the only generation flow.
- Privacy posture stays aligned with the current product.
</success_criteria>

<output>
After completion, create `.planning/phases/05-creator-paid-brief-templates-shareable-prefilled-pages/05-03-SUMMARY.md`
</output>
