---
phase: 999.6
plan: 02
type: execute
wave: 2
depends_on: ["999.6-03"]
files_modified:
  - web/scripts/fetch-landtag-politicians.ts
  - web/scripts/fetch-politician-data.ts
  - web/package.json
autonomous: true
requirements: ["POLI-10"]
must_haves:
  truths:
    - "All 16 Landtag parliament_period IDs are resolved dynamically from /parliament-periods at build time."
    - "All Landtag mandates are fetched sequentially with 200ms delay and merged into politicians-cache.landtag[]."
    - "Each Landtag politician has a postalAddress derived from landtag-addresses.json keyed by Bundesland code, and level=\"Land\"."
    - "Each Landtag politician has a bundeslandKey field so the runtime lookup can route by Land."
  artifacts:
    - path: "web/scripts/fetch-landtag-politicians.ts"
      provides: "Build-time fetcher: AW v2 → all 16 Landtage → cache extension"
    - path: "web/data/politicians-cache.json"
      provides: "Existing file, .landtag[] populated with ≥1500 entries spanning all 16 parliaments"
  key_links:
    - from: "web/scripts/fetch-landtag-politicians.ts"
      to: "https://www.abgeordnetenwatch.de/api/v2/parliament-periods"
      via: "native fetch with retry, sorted by start_date_period desc, type=legislature"
      pattern: "/parliament-periods\\?parliament="
    - from: "web/scripts/fetch-landtag-politicians.ts"
      to: "web/data/landtag-addresses.json"
      via: "fs.readFileSync at build time, keyed by Bundesland code"
      pattern: "landtag-addresses\\.json"
---

<objective>
Extend the existing build-time politician fetcher to cover all 16 Landtage via Abgeordnetenwatch v2, populating `politicians-cache.landtag[]` with full mandate data and institutional postal addresses.

Purpose: Sub-phase A's core data layer. The Bund cache exists; this plan adds the Land tier so PLAN 04's level router has politicians to route to and PLAN 05's badge has data to display.

Output: One new build script + an updated `politicians-cache.json` with `.landtag[]` populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@web/CLAUDE.md
@.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-CONTEXT.md
@.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-RESEARCH.md
@web/scripts/fetch-politician-data.ts
@web/src/lib/types/politician.ts

<interfaces>
Existing `Politician` type (web/src/lib/types/politician.ts):
```typescript
export type PoliticalLevel = "Bund" | "Land" | "Kommune";
export interface Politician {
  id: number; politicianId: number;
  firstName: string; lastName: string; title: string | null;
  party: string;
  wahlkreisId: number; wahlkreisName: string;
  level: PoliticalLevel;
  postalAddress: string;
  isDirect: boolean;
  abgeordnetenwatchUrl: string | null;
  committees?: string[];
}
export interface PoliticiansCache {
  bundestag: Politician[]; landtag: Politician[]; kommune: Politician[]; lastUpdated: string;
}
```

Reusable helpers in `fetch-politician-data.ts` (Plan 02 should EXPORT them rather than copy):
- `fetchWithRetry(url, retries)` — generic retrying fetch
- `fetchAllPages(endpoint)` — paginates AW v2
- `splitName(label)` — title/firstName/lastName splitter
- `parseConstituencyLabel(label)` — "56 - Oberhausen I (NRW 2022 - 2027)" → { nr, name }
- `extractParty(mandate)` — fraction membership label cleaner
- `sleep(ms)`, `mapWithConcurrency`

Parliament IDs (RESEARCH §1.2): 2=BE, 3=HH, 4=NW, 6=BW, 7=RP, 8=ST, 9=MV, 10=HB, 11=HE, 12=NI, 13=BY, 14=SL, 15=TH, 16=BB, 17=SN, 18=SH. Parliament 5=Bundestag is excluded here (handled by the existing fetcher).

Period resolution contract (D4, RESEARCH §1.2):
GET /parliament-periods?parliament={id} → array of `{ id, type, start_date_period, label }`. Filter `type === "legislature"`, sort by `start_date_period` DESC, take `[0].id`.

Bundesland code mapping (must match landtag-addresses.json keys from PLAN 03):
| Parliament ID | Code | Name |
|---|---|---|
| 2 | BE | Berlin |
| 3 | HH | Hamburg |
| 4 | NW | Nordrhein-Westfalen |
| 6 | BW | Baden-Württemberg |
| 7 | RP | Rheinland-Pfalz |
| 8 | ST | Sachsen-Anhalt |
| 9 | MV | Mecklenburg-Vorpommern |
| 10 | HB | Bremen |
| 11 | HE | Hessen |
| 12 | NI | Niedersachsen |
| 13 | BY | Bayern |
| 14 | SL | Saarland |
| 15 | TH | Thüringen |
| 16 | BB | Brandenburg |
| 17 | SN | Sachsen |
| 18 | SH | Schleswig-Holstein |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor shared helpers + extend Politician type with bundeslandKey</name>
  <read_first>
    - web/scripts/fetch-politician-data.ts (complete file — to extract helpers)
    - web/src/lib/types/politician.ts (full type definition)
  </read_first>
  <action>
    Two edits, both small:

    1. In `web/src/lib/types/politician.ts`, add an optional `bundeslandKey?: string` field to the `Politician` interface, immediately after `level`. The comment should read: `// ISO 3166-2:DE code without "DE-" prefix (e.g. "NW", "BY"). Required for level=="Land", optional for "Bund".`

    2. In `web/scripts/fetch-politician-data.ts`, change the following from `function` to `export function`:
       - `sleep`
       - `fetchWithRetry`
       - `fetchAllPages`
       - `splitName`
       - `parseConstituencyLabel`
       - `extractParty`
       - `fetchCommittees`
       - `mapWithConcurrency`

       Also export the constant `API_BASE`. Do NOT change `main()` or its invocation; the existing Bundestag fetch must continue to work unchanged.

    Per D11, no new dependencies. Per project rule (web/AGENTS.md): if Next.js or Node API surface has changed, consult `node_modules/next/dist/docs/` or relevant Node 20+ docs before edits — but this task only touches plain TypeScript with stdlib + native fetch, so no API drift risk.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function" web/scripts/fetch-politician-data.ts` returns ≥ 7.
    - `grep -c "bundeslandKey" web/src/lib/types/politician.ts` returns ≥ 1.
    - `npx tsc --noEmit` passes (existing Bundestag fetch still type-checks).
    - `git diff` shows no changes outside the two listed files.
  </acceptance_criteria>
  <done>Helpers are exported, type extended, project still type-checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build script — fetch all 16 Landtage with dynamic period resolution</name>
  <read_first>
    - web/scripts/fetch-politician-data.ts (post-Task-1 — the now-exported helpers and main flow)
    - web/data/politicians-cache.json (existing shape — must preserve `bundestag`, `kommune`, `lastUpdated`)
    - .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-RESEARCH.md §1.2 (period IDs, constituency label format)
  </read_first>
  <action>
    Create `web/scripts/fetch-landtag-politicians.ts`.

    1. Imports (no new deps, D11):
       ```typescript
       import * as fs from "fs";
       import * as path from "path";
       import {
         API_BASE, sleep, fetchWithRetry, fetchAllPages,
         splitName, parseConstituencyLabel, extractParty,
       } from "./fetch-politician-data";
       import type { Politician, PoliticiansCache } from "../src/lib/types/politician";
       ```

    2. Define the 16-row mapping table exactly as listed in `<interfaces>` above as `const LANDTAGE: ReadonlyArray<{ parliamentId: number; code: string; name: string }>`.

    3. Load `web/data/landtag-addresses.json` once (produced by PLAN 03). Type it as `Record<string, { name: string; address: string }>`. If the file is missing at run time, log a warning and use a placeholder `"[Adresse fehlt — PLAN 03 nicht ausgeführt]"` rather than failing. Production builds will only run after PLAN 03 ships.

    4. Implement `async function resolveCurrentPeriod(parliamentId: number): Promise<{ id: number; label: string } | null>`:
       - GET `${API_BASE}/parliament-periods?parliament=${parliamentId}&range_end=50`
       - Filter `data.type === "legislature"`.
       - Sort by `data.start_date_period` (string ISO date) DESC.
       - Return `{ id: data[0].id, label: data[0].label }` or `null` if empty.

    5. Implement `async function fetchLandtagMandates(parliamentId: number, periodId: number, code: string): Promise<Politician[]>`:
       - Reuse `fetchAllPages('candidacies-mandates?parliament_period=' + periodId + '&type=mandate')`.
       - Map each mandate via the same helper logic as Bundestag, but set:
         - `level: "Land"`
         - `bundeslandKey: code`
         - `postalAddress: addresses[code]?.address ?? "[Adresse fehlt]"`
       - Skip mandates without a constituency (same rule as Bundestag).
       - Set `wahlkreisId` from `parseConstituencyLabel(...).nr`. Note these IDs are per-period; that is acceptable because PLAN 01 and PLAN 04 will match by Land+ID, never by ID alone.

    6. Sequential fetch loop in `main()`: for each entry in LANDTAGE, log `[landtag] {code} parliament={id}: resolving period…`, resolve period, then fetch mandates. After EACH parliament, `await sleep(200)` (per D9). Skip committee fetch (out of scope for v1 Landtag — keeps build time bounded).

    7. After all 16 are fetched, READ existing `web/data/politicians-cache.json`, REPLACE the `landtag` array with the new combined list, KEEP the existing `bundestag` and `kommune` arrays untouched, UPDATE `lastUpdated` to `new Date().toISOString()`. Write the file with 2-space indent.

    8. Console summary at end: per-Land count `{code}: {n} mandates`, plus total Landtag entries.

    9. Wire into `web/package.json`:
       `"fetch:landtag": "tsx scripts/fetch-landtag-politicians.ts"`

    Honor `web/CLAUDE.md` rule: if any Node/Next API used here is suspect, consult `node_modules/next/dist/docs/`. This script uses only `fs`, `path`, native fetch — no Next.js APIs — so no doc check needed.

    Defensive note (Pitfall 1, RESEARCH): never hardcode period or constituency IDs. The dynamic resolver is the only path. If `resolveCurrentPeriod` returns null for a given parliament, log `[landtag] {code}: no current legislature period — SKIPPED` and continue rather than failing.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run fetch:landtag 2>&amp;1 | tail -30 &amp;&amp; node -e "const c=require('./data/politicians-cache.json');console.log('landtag='+c.landtag.length);process.exit(c.landtag.length>=1500?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `web/scripts/fetch-landtag-politicians.ts` exists.
    - `grep -c "fetch:landtag" web/package.json` returns ≥ 1.
    - `grep -c "parliament-periods" web/scripts/fetch-landtag-politicians.ts` returns ≥ 1 (D4 dynamic resolution).
    - `grep -c "sleep(200)" web/scripts/fetch-landtag-politicians.ts` returns ≥ 1 (D9 delay).
    - `grep -c "type=legislature\\|=== \"legislature\"" web/scripts/fetch-landtag-politicians.ts` returns ≥ 1.
    - After running, `politicians-cache.json` `.landtag.length >= 1500` (16 Landtage × ~100 average = ~1900 expected; floor at 1500 to allow for partial failures).
    - Every entry in `.landtag` has `level === "Land"` and a non-empty `bundeslandKey`.
    - `.bundestag.length` is unchanged from before the run (existing Bund data preserved).
    - `grep -c "^import.*from.*csv-parse\\|^import.*axios\\|^import.*node-fetch" web/scripts/fetch-landtag-politicians.ts` returns 0 (D11 — no new deps).
  </acceptance_criteria>
  <done>`npm run fetch:landtag` writes ≥1500 Landtag mandates into `politicians-cache.json` while preserving the Bundestag tier.</done>
</task>

</tasks>

<verification>
- `politicians-cache.json` schema unchanged at the top level; `.landtag[]` newly populated.
- All Landtag entries have `level=="Land"`, `bundeslandKey` populated, `postalAddress` non-empty.
- `npm run fetch:politicians && npm run fetch:landtag` is the canonical full-rebuild sequence (D9: full rebuild per deploy).
</verification>

<success_criteria>
- ≥14 of 16 Landtage successfully fetched (allow up to 2 failures for parliament-period edge cases).
- No new runtime npm deps (D11).
- Helpers are reused via export, not copy-pasted.
</success_criteria>

<output>
After completion, create `.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-02-SUMMARY.md`.
</output>
