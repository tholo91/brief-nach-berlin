---
phase: 999.6
plan: 06
type: execute
wave: 4
depends_on: ["999.6-04"]
files_modified:
  - web/src/lib/generation/generateLetter.ts
  - web/src/lib/actions/submitWizard.ts
  - web/src/lib/email/buildDebugPayload.ts
  - web/src/lib/email/buildEmailHtml.ts
  - web/src/lib/email/sendLetterEmail.ts
  - web/src/__tests__/generateLetter.test.ts
  - web/src/__tests__/buildEmailHtml.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "generateLetter accepts an explicit `level` parameter ('Bund'|'Land'|'Kommune') plus an optional `mode` ('primary'|'secondary-stellvertretend') and adapts Anrede, Framing, and Begründung accordingly (D12). **'Kommune' not 'Gemeinde'** — canonical enum matches PoliticalLevel (review B3)."
    - "The Anrede block uses resolved gender for Bund + Land ('Sehr geehrter Herr Abgeordneter' / 'Sehr geehrte Frau Abgeordnete' — review W10, no slash). For Kommune the Anrede is intentionally generic (Thomas-Decision 2026-05-22): 'Sehr geehrte Damen und Herren der Stadtverwaltung,' (or 'des Bezirksamts,' for Berlin). Stadtstaaten Land uses Senator/Senatorin/Erster Bürgermeister via formal_title. Slash-form ONLY as last-resort for Bund/Land when gender unknown."
    - "Per-level Brief-Strategie ist materially unterschiedlich (Thomas-Decision 2026-05-22, NOT just Anrede-Swap): Bund=strukturelle Argumentation+Fraktions-/Ausschuss-Hebel, Land=Landeskompetenz+konkrete Landesinstrumente, Kommune=lokale Direktheit+Pflichtaufgaben+lösungs-orientiert (weniger ideologisch). System-Prompt-Branches enthalten je einen eigenen Strategy-Block mit Formalität-Stufe, Nachdruck-Hebel, Argumentations-Muster."
    - "Letter-Email enthält einen sichtbaren Routing-Footer (D17) am Ende: 'Brief gerichtet an: [Bund|Land|Kommune]-Ebene. KI-Vorauswahl: ... (Confidence: ...). [Override-Hinweis falls applicable].' Plus erweiterte debug-URL-Payload mit routedPrimary/routedSecondary/wasOverridden/selectedLevel."
    - "When `mode='secondary-stellvertretend'`, the letter explicitly acknowledges the constitutional competence-mismatch (Thomas-Decision 2026-05-21): 'Ich weiß, dass Sie als Bundestagsabgeordnete für [Themenfeld] nicht direkt zuständig sind, diese liegt bei den Ländern. Trotzdem schreibe ich Ihnen, weil ...'. No false promises of MdB action-capability (review B7)."
    - "**No GG-Artikelnummern** in the letter body (Thomas-Decision 2026-05-21). Prompt explicitly forbids citing 'Art. X GG' patterns. LLM-Halluzinations-Risk too high (review W11)."
    - "Stadtstaaten (HH/HB/BE) have special handling: Land = Senator/in or Bürgermeister-as-Senatsmitglied. Kommune does not exist constitutionally (Einheitsgemeinde Art. 28 Abs. 1 S. 2 GG). Berliner Bezirksbürgermeister as edge case via Bezirksverwaltungsgesetz Berlin (review W10)."
    - "Existing letter quality (200–280 Wörter, formell, kein Em-Dash, Sie-Form) is preserved."
  artifacts:
    - path: "web/src/lib/generation/generateLetter.ts"
      provides: "Level-aware letter generator with primary/secondary modes"
      exports: ["generateLetter", "type LetterLevel", "type LetterMode"]
  key_links:
    - from: "web/src/lib/actions/submitWizard.ts"
      to: "generateLetter(opts)"
      via: "passes routedPrimary.level (or selected politician's level) plus mode='primary'|'secondary-stellvertretend'"
      pattern: "generateLetter"
---

<objective>
Make the letter content actually different per political level. Today's prompt produces one generic structure regardless of whether the recipient is an MdB, MdL, or Bürgermeister/in. After this plan, the prompt branches: federal letters argue structurally and reference national patterns, state letters reference state-level competence (and GG when relevant), and the special "stellvertretend" mode (when a user picks the secondary recipient from D7) explicitly frames the issue as a regional example deserving the wider stage.

Purpose: D12 (newly in scope per the Discuss-Phase update on 2026-05-09) requires that the letter prompt adapts. Without this plan, PLAN 04's level routing produces the right recipient but the wrong letter content.

Output: One refactored generator function + one extended action call + extended test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@web/CLAUDE.md
@.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-CONTEXT.md
@web/src/lib/generation/generateLetter.ts
@web/src/lib/actions/submitWizard.ts
@web/src/lib/types/politician.ts

<interfaces>
The existing `generateLetter` produces a system prompt with one fixed Anrede block ("Sehr geehrte/r [Titel] [Name]"). After this plan it accepts:

```typescript
export type LetterLevel = "Bund" | "Land" | "Kommune"; // canonical, matches PoliticalLevel (review B3)
export type LetterMode = "primary" | "secondary-stellvertretend";

export interface GenerateLetterOptions {
  // existing fields preserved...
  anliegen: string;
  politician: Politician; // must now include gender (m/f/d) + formal_title (review W10)
  level: LetterLevel;          // NEW
  mode?: LetterMode;            // NEW, defaults to "primary"
}
```

**Politician shape extension (review W10):** Update `web/src/lib/types/politician.ts` to add:
```typescript
gender?: "m" | "f" | "d";          // populated from AW politician.field_gender if available
formal_title?: string;              // populated from AW politician.field_title or computed for Kommune
                                    // (e.g. "Oberbürgermeisterin", "Erster Bürgermeister", "Senator")
bundeslandKey?: string;             // already added in PLAN 02 for Land routing — used here for Stadtstaaten detection
```

Anrede mapping (review W10 — no slash-form unless gender unknown; revised 2026-05-22 for Kommune):
```
gender + level → Anrede:
- gender=m, level=Bund/Land           → "Sehr geehrter Herr Abgeordneter [Nachname]"
- gender=f, level=Bund/Land           → "Sehr geehrte Frau Abgeordnete [Nachname]"
- gender=d, level=Bund/Land           → "Sehr geehrte:r [Nachname]"
- level=Kommune (alle Gemeinden!)     → "Sehr geehrte Damen und Herren der Stadtverwaltung," (Thomas-Decision 2026-05-22, see D13)
- level=Kommune, recipientKind=bezirksamt → "Sehr geehrte Damen und Herren des Bezirksamts,"
- gender unknown (Bund/Land)          → last-resort slash: "Sehr geehrte/r [Anrede] [Nachname]"
```

**Kommune is intentionally generic (D13 Thomas-Decision 2026-05-22):**
- Kein Bürgermeister-Name, kein Gender-Resolve, kein `formal_title` für die Anrede selbst.
- Adresse-Block enthält den `formal_title` als Empfänger-Label ("Stadtverwaltung Köln, Stadt").
- 100% Coverage über alle ~11.000 Gemeinden, kein Wikidata-Lookup nötig.

**Stadtstaaten special handling (review W10):**
- `bundeslandKey ∈ {HH, HB, BE}` + `level=Land` → resolved gender + formal_title (Senator, Senatorin, Erster Bürgermeister, Bürgermeisterin).
- `bundeslandKey ∈ {HH, HB}` + `level=Kommune` → routeToLevel/lookupPLZWithLevel re-routet auf Land (Einheitsgemeinde, Art. 28 Abs. 1 S. 2 GG). Wird gar nicht erst als Kommune-Brief erreicht.
- `bundeslandKey = BE` + `level=Kommune` → Bezirksamt-Variante: Anrede `"Sehr geehrte Damen und Herren des Bezirksamts,"`, formal_title `"Bezirksamt Berlin, [Bezirksname]"`.

Framing mapping — **Strategie + Nachdruck + Formalität + Argumentation per Ebene** (revised 2026-05-22 per Thomas-Decision, expanded from prior "wo kommt's her" to full Brief-Strategie):

**Bund (Bundestagsabgeordnete):**
- **Strategie:** Strukturelle Argumentation, bundesweite Muster, Verweis auf Gesetzgebungs-Kompetenz oder Bundesfinanzierung. Der Brief positioniert das Anliegen als Teil eines systemischen Problems, nicht als Einzelfall.
- **Formalität:** Hohe formelle Distanz. Empfänger ist Repräsentant:in mit ca. 250-300k Wahlkreis-Bevölkerung. Brief muss in 2 Minuten Lese-Zeit punkten und sich vom Petitions-Tonfall absetzen.
- **Nachdruck-Hebel:** Hinweis auf Fraktions-Position oder Ausschuss-Mitgliedschaft (wenn MdB-Kontext mitgeliefert), Verweis auf eigene Wähler:in-Schaft im Wahlkreis, konkrete Forderung mit Bundes-Hebel (Bundesgesetz / Förderprogramm / Sondervermögen / Beauftragung).
- **Argumentations-Muster:** "Dieses Problem betrifft viele Regionen — das macht eine bundesweite Lösung notwendig" ODER "Hier braucht es einen Bundesrahmen, damit Länder nicht uneinheitlich umsetzen".
- **Verboten:** GG-Artikelnummern zitieren (review W11, Thomas-Decision). Allgemeine Begriffe wie "Bundeskompetenz" oder "Sozialgesetzbuch" sind OK.

**Land (Landtagsabgeordnete):**
- **Strategie:** Landesspezifische Verantwortlichkeit, konkrete Landesinstrumente (Landesschulgesetz, Landespolizeigesetz, Krankenhausplan). Argumentation in Landes-Logik, nicht Bundes-Logik.
- **Formalität:** Mittlere formelle Distanz. Empfänger ist näher an der Wähler:in-Schaft als ein MdB (kleinerer Wahlkreis). Tonfall darf etwas konkreter, regionaler sein.
- **Nachdruck-Hebel:** Bezug zum konkreten Wahlkreis / Stadtteil / Ort, Verweis auf Landtags-Ausschuss-Befassung, Bürger:in-Status im Wahlkreis. Wenn passend: aktuelle Landespolitische Debatte aufgreifen.
- **Argumentations-Muster:** "[Bundesland] hat hier eigene Kompetenz und Gestaltungsspielraum. Ich erwarte, dass…" ODER "Im Bundesvergleich liegt [Bundesland] zurück / vorn — das sollte adressiert werden".
- **Verboten:** Bundesgesetzgebungs-Forderungen (das wäre Bund-Sache). Keine GG-Artikelnummern.

**Kommune (Stadtverwaltung / Bezirksamt):**
- **Strategie:** Lokale Direktheit, konkrete Verwaltungs-Einheit (Bauamt, Ordnungsamt, Verkehrsbehörde), kurzer Weg zur Lösung. Der Brief richtet sich an die Verwaltung als Ganzes, nicht an eine Einzelperson.
- **Formalität:** Geringere formelle Distanz als Bund/Land, aber weiterhin Sie-Form. Tonfall konkret, sachlich, alltagsorientiert. Empfänger ist kein Politiker mit Wahlkampf-Logik, sondern Verwaltung — also weniger ideologisch, mehr lösungs-orientiert argumentieren.
- **Nachdruck-Hebel:** Konkrete Orts-/Straßen-Angabe, Verweis auf Pflichtaufgaben der Gemeinde (Bauleitplanung, Straßenunterhaltung, ÖPNV-Zuständigkeit), Bürger:in-Status in der Gemeinde. Bei Untätigkeit: Hinweis auf öffentliche Sichtbarkeit (Lokalpresse, Stadtteil-Versammlung) als legitimen demokratischen Druck.
- **Argumentations-Muster:** "Als Bürger:in in [Ortsname] erwarte ich, dass die Stadtverwaltung [konkrete Forderung]. Diese Aufgabe liegt in der kommunalen Selbstverwaltung."
- **Verboten:** Politik-Floskel-Sprache (Mehrwert, Maßnahmenpaket, Synergien). Briefe an Verwaltung müssen NICHT politisch klingen — sie müssen handlungs-orientiert klingen.

**Stellvertretend-Mode (mode='secondary-stellvertretend', Thomas-Decision 2026-05-21):**
- Bund + secondary: explizite Anerkennung der Kompetenz-Asymmetrie. Eröffnung: *"Sehr geehrte Frau Abgeordnete Müller, ich weiß, dass Sie als Bundestagsabgeordnete für [Bildungspolitik] nicht direkt zuständig sind, diese liegt bei den Ländern. Trotzdem wende ich mich an Sie, weil [User-Begründung aus Anliegen]."*
- Land + secondary: analoge explizite Anerkennung.
- Kommune + secondary: in v1 nicht erreicht — Stadtverwaltung ist immer primary für Kommune-Anliegen.

**Email-Debug-Footer (D17, NEU 2026-05-22):**
- Am Ende der Letter-Email (vor der Signatur, in einer dezenten grauen Box): *"Brief gerichtet an: [Bund | Land | Kommune]-Ebene. KI-Vorauswahl: [Bund | Land | Kommune] (Confidence: [high | medium | low]). [Falls Override: 'Du hast die Ebene manuell auf [Level] gewechselt.']"*
- Plus die existierende Debug-URL am Mail-Ende (buildDebugPayload erweitert um routedPrimary/routedSecondary/wasOverridden/selectedLevel — siehe Task 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor generateLetter to accept level + mode and branch the system prompt</name>
  <read_first>
    - web/src/lib/generation/generateLetter.ts (full file — current system prompt structure, Mistral call, JSON parse pattern)
    - web/src/lib/types/politician.ts (Politician shape, level field)
  </read_first>
  <behavior>
    - Test 1: level="Bund", mode="primary", gender="f" → system prompt contains "Sehr geehrte Frau Abgeordnete" (no slash), Bund-Strategie-Block (strukturelle Argumentation, Fraktion/Ausschuss-Hebel), no "Art. X GG" pattern.
    - Test 2: level="Land", mode="primary", gender="m" → system prompt contains "Sehr geehrter Herr Abgeordneter", Land-Strategie-Block (Landeskompetenz, konkrete Landesinstrumente, mittlere Formalität), NO "Art. 70 GG" or any Article-number citation (review W11).
    - Test 3: level="Kommune", mode="primary", recipient.isSynthetic=true (Stadtverwaltung) → system prompt contains EXACTLY "Sehr geehrte Damen und Herren der Stadtverwaltung" (generic, no Bürgermeister-Name, no gender-resolve), Kommune-Strategie-Block (lokale Direktheit, Pflichtaufgaben, weniger ideologisch).
    - Test 4: level="Kommune", recipient.recipientKind="bezirksamt" (Berlin) → system prompt contains "Sehr geehrte Damen und Herren des Bezirksamts".
    - Test 5: level="Land", bundeslandKey="HH", gender="m", formal_title="Senator" → "Sehr geehrter Herr Senator" (Stadtstaat handling, review W10).
    - Test 6: level="Bund", mode="secondary-stellvertretend" → system prompt contains "ich weiß, dass Sie als" + "nicht direkt zuständig sind" (explicit competence-mismatch acknowledgment, review B7, Thomas-Decision).
    - Test 7: gender unknown (Bund/Land) → falls back to slash-form "Sehr geehrte/r" (last-resort, review W10). Does NOT trigger for Kommune (Kommune is always generic-form).
    - Test 8: Default mode (omitted) behaves identically to mode="primary".
    - Test 9: Output letter still satisfies existing structure (Anrede, Hauptteil, Schluss, Unterschrift).
    - Test 10 (review W11): For ANY level + ANY mode, the constructed system prompt MUST NOT contain "Art. 70 GG", "Art. 30 GG", "Art. 28 GG", or any "Art. X GG" pattern. Letter-text must also not contain these — assert via regex `/Art\.\s*\d+/i` returns no match in mocked output.
    - Test 11: **Strategy-block presence (Thomas-Decision 2026-05-22):** For each of `level=Bund / Land / Kommune`, the system prompt MUST contain a distinct strategy-block keyed to that level. Assert: Bund-Branch contains "strukturelle" or "Fraktion" or "Bundesgesetz"; Land-Branch contains "Landeskompetenz" or "Landesinstrumente"; Kommune-Branch contains "Pflichtaufgaben" or "lokale Direktheit" or "Verwaltung". The three strategy blocks must be materially different (not just the Anrede swap).
  </behavior>
  <action>
    Refactor `web/src/lib/generation/generateLetter.ts`:

    1. Add the new types `LetterLevel = "Bund" | "Land" | "Kommune"` (review B3), `LetterMode`, and extend `GenerateLetterOptions`.
    2. Extract the current system-prompt string into a builder function `buildSystemPrompt(opts: GenerateLetterOptions): string` that branches on `opts.level`, `opts.mode`, `opts.politician.gender`, `opts.politician.formal_title`, and `opts.politician.bundeslandKey` (for Stadtstaat detection).
    3. **Anrede mapping (review W10):** use resolved `gender` + `formal_title` to emit non-slash form. Only fall back to slash-form `"Sehr geehrte/r"` when `gender` is unknown.
       - For Bund + Land: `"Sehr geehrter Herr Abgeordneter [Nachname]"` / `"Sehr geehrte Frau Abgeordnete [Nachname]"`
       - For Kommune: use `formal_title` directly (`"Sehr geehrter Herr Oberbürgermeister [Nachname]"`, `"Sehr geehrte Frau Erste Bürgermeisterin [Nachname]"`)
       - For Stadtstaaten: Land = `formal_title` (Senator/in, Erster Bürgermeister); Kommune in HH/HB rejects (Einheitsgemeinde); Kommune in BE uses Bezirksbürgermeister-Logik.
    4. **Framing branches** keyed by `level`:
       - Bund: bundesweite Argumentation, Bundesgesetz-Bezug, **NO GG-Artikelnummern**.
       - Land: landesspezifische Verantwortlichkeit, allgemeine Begriffe wie "Kulturhoheit der Länder" OK, **NO Art. X GG citations** (review W11, Thomas-Decision).
       - Kommune: lokale Direktheit, konkrete Verwaltungseinheit, Rathaus / Stadtrat / Bezirksamt-Bezug.
       - System-prompt MUST contain: *"Zitiere KEINE Grundgesetz-Artikel mit Nummern (kein 'Art. 70 GG', kein 'Art. 28 GG', etc.). Allgemeine Begriffe wie 'Kulturhoheit der Länder' oder 'kommunale Selbstverwaltung' sind erlaubt."*
    5. **Stellvertretend-Mode (review B7, Thomas-Decision):** when `mode === "secondary-stellvertretend"`, **prepend** the Eröffnungs-Absatz with an explicit competence-mismatch acknowledgment (no false promises). Example template:
       ```
       "Sehr geehrte [Anrede] [Nachname],

       ich weiß, dass Sie als [Bundestagsabgeordnete | Landtagsabgeordnete] für [Themenfeld aus Anliegen] nicht direkt zuständig sind, diese liegt bei [den Ländern | dem Bund | der Kommune]. Trotzdem wende ich mich an Sie, weil [User-Begründung aus Anliegen]."
       ```
       Then the rest of the letter argues from user-agency, not from MdB-Handlungsfähigkeit.
    6. Keep the existing word target (200–280) and tone rules. Keep the no-em-dash project rule (use commas/colons).
    7. Update the function signature; preserve backwards compatibility by defaulting `mode` to `"primary"`.
    8. Extend tests in `web/src/__tests__/generateLetter.test.ts` (or create one if missing — match existing test conventions). Mock Mistral; assert on the constructed system prompt AND on the mocked output. Cover all 10 behaviors above.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; (npx vitest run src/__tests__/generateLetter.test.ts 2>/dev/null || npm test -- --run src/__tests__/generateLetter.test.ts 2>/dev/null || true)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LetterLevel\\|LetterMode" web/src/lib/generation/generateLetter.ts` returns ≥ 2.
    - `grep -c "\"Kommune\"" web/src/lib/generation/generateLetter.ts` returns ≥ 1 AND `grep -c "\"Gemeinde\"" web/src/lib/generation/generateLetter.ts` returns 0 (canonical enum, review B3).
    - `grep -c "Abgeordneter\\|Abgeordnete\\|Senator\\|Senatorin\\|Stadtverwaltung\\|Bezirksamt" web/src/lib/generation/generateLetter.ts` returns ≥ 4 (no-slash Anrede branches + generic Kommune, review W10 + D13).
    - `grep -c "Sehr geehrte Damen und Herren der Stadtverwaltung" web/src/lib/generation/generateLetter.ts` returns ≥ 1 (Kommune generic Anrede, Thomas-Decision 2026-05-22).
    - `grep -c "secondary-stellvertretend\\|nicht direkt zuständig" web/src/lib/generation/generateLetter.ts` returns ≥ 2 (review B7).
    - `grep -c "Art\\.\\s*[0-9]\\+\\s*GG" web/src/lib/generation/generateLetter.ts` returns **0** (no GG-Artikelnummern in prompt or template strings, review W11).
    - System prompt CONTAINS the rule "Zitiere KEINE Grundgesetz-Artikel mit Nummern" (test asserts on `buildSystemPrompt` output for any level).
    - **Strategy-block presence (Thomas-Decision 2026-05-22):** the Bund-Branch contains "strukturelle" or "Fraktion" or "Bundesgesetz"; Land-Branch contains "Landeskompetenz" or "Landesinstrumente"; Kommune-Branch contains "Pflichtaufgaben" or "lokale Direktheit" or "Verwaltung". The strategy-blocks are materially different (Behavior Test 11 enforces this).
    - `grep -c "—" web/src/lib/generation/generateLetter.ts` returns 0.
    - `npx tsc --noEmit` passes.
    - All 11 behaviors covered by tests, all pass.
  </acceptance_criteria>
  <done>generateLetter is level-aware, gender-aware, Stadtstaat-aware, GG-citation-free, and the Stellvertretend-Mode owns its competence-mismatch transparently. The prompt visibly branches by level + mode + gender + formal_title.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire level + mode into submitWizard letter call</name>
  <read_first>
    - web/src/lib/actions/submitWizard.ts (full file — find the existing generateLetter call site)
    - web/src/lib/actions/selectPolitician.ts (if letter generation runs in this action instead)
  </read_first>
  <action>
    At the existing `generateLetter(...)` call site:
    1. Pass `level: selectedPolitician.level` (the actual level of the politician the user picked).
    2. Compute `mode`: if the user selected a politician from the secondary group (track via a flag `wasSecondarySelection` in the action input or by comparing `selectedPolitician.level !== routedPrimary.level`), pass `mode: "secondary-stellvertretend"`; otherwise `mode: "primary"` (or omit).
    3. Add a single log line capturing `{ level, mode, politicianId }` for production observability.

    Keep all other behaviors unchanged — this is a 5–10 line change.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; grep -c "level:\\s*[a-zA-Z]\\+\\.level\\|mode:\\s*['\"]" web/src/lib/actions/submitWizard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "level:.*\\.level\\|level: selectedPolitician\\.level" web/src/lib/actions/submitWizard.ts` returns ≥ 1.
    - `grep -c "secondary-stellvertretend\\|mode:" web/src/lib/actions/submitWizard.ts` returns ≥ 1.
    - `npx tsc --noEmit` passes.
    - Existing tests still pass.
  </acceptance_criteria>
  <done>The action passes the right level + mode into the generator, and we can observe both in production logs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend Email Debug-Payload + visible Routing-Footer (D17)</name>
  <read_first>
    - web/src/lib/email/buildDebugPayload.ts (existing payload with politicalLevel — extend)
    - web/src/lib/email/buildEmailHtml.ts (existing email body, find a good spot for the visible footer)
    - web/src/lib/email/sendLetterEmail.ts (LetterDebugPayload + SendLetterEmailParams types)
    - web/src/lib/actions/submitWizard.ts (where sendLetterEmail is called — what data is available)
  </read_first>
  <action>
    1. **Extend `LetterDebugPayload`** in `sendLetterEmail.ts` (or wherever the type lives) with:
       ```typescript
       interface LetterDebugPayload {
         // existing fields preserved...
         // NEW (D17):
         routedPrimaryLevel?: "Bund" | "Land" | "Kommune" | null;     // null when router timed out
         routedPrimaryConfidence?: "high" | "medium" | "low" | null;
         routedSecondaryLevel?: "Bund" | "Land" | "Kommune" | null;
         wasOverridden?: boolean;       // true if user changed level via override chips
         selectedLevel?: "Bund" | "Land" | "Kommune";  // level of the recipient actually written to
       }
       ```

    2. **Extend `buildDebugPayload`** in `buildDebugPayload.ts` to populate these from the submitWizard caller. SubmitWizard must pass the routing-info into `sendLetterEmail`:
       ```typescript
       // in submitWizard.ts at the sendLetterEmail call:
       await sendLetterEmail({
         // ... existing params ...
         routing: {
           routedPrimary: routing?.primary ?? null,
           routedSecondary: routing?.secondary ?? null,
           wasOverridden,                   // computed from override-chip state in result vs. routing.primary.level
           selectedLevel: selectedPolitician.level,
         },
       });
       ```
       Define a `LetterRoutingInfo` type for these 4 fields and add it to `SendLetterEmailParams`.

    3. **Add visible footer to the email body** in `buildEmailHtml.ts`. Find the appropriate spot — likely after the letter body, before the existing debug-URL footer or the feedback star-bar (read the file first to choose the cleanest position). Insert a small gray HTML block:
       ```html
       <div style="margin:24px 0 16px;padding:12px 16px;background:#f5f3ee;border:1px solid #d4cfc1;border-radius:6px;font-family:Georgia,'Times New Roman',serif;font-size:11px;color:#6b6963;line-height:1.5;">
         <strong style="color:#2d5016;">Wohin geht dein Brief?</strong><br>
         An die <strong>${routingFooterLevel}</strong>-Ebene
         (${routingFooterRecipientLabel}).
         ${routingFooterAiHint}
         ${routingFooterOverrideHint}
       </div>
       ```
       Where:
       - `routingFooterLevel`: "Bundes" / "Landes" / "Kommunal" (lay-form of the level)
       - `routingFooterRecipientLabel`: "Bundestagsabgeordnete:r" / "Landtagsabgeordnete:r" / "Stadtverwaltung [Ortsname]"
       - `routingFooterAiHint`: if router result available: `KI-Vorauswahl: ${routedPrimaryLevel}-Ebene (Sicherheit: ${routedPrimaryConfidence}).`. If timeout/null: empty.
       - `routingFooterOverrideHint`: if `wasOverridden`: `Du hast die Ebene manuell auf ${selectedLevel} gewechselt.`. Else empty.

       Keep it under 4 lines visually. No CTA buttons. Mail-client-safe inline styles (mirror existing pattern in `buildEmailHtml.ts`).

    4. **Provide a helper** `buildRoutingFooterHtml(routing: LetterRoutingInfo, recipient: { name: string; level: PoliticalLevel; isSynthetic?: boolean }): string` that takes the routing info + recipient and returns the HTML snippet. Unit-test it for the 4 cases:
       - level=Bund, routedPrimary=Bund, no override
       - level=Land, routedPrimary=Land, no override
       - level=Kommune, routedPrimary=Kommune, recipient.isSynthetic=true → "Stadtverwaltung [Ortsname]" as recipient label
       - level=Bund, routedPrimary=Land, wasOverridden=true → shows override hint
       - routing=null (router failed/timed out) → graceful: shows only the level, no AI-hint

    5. **Tests:** extend `web/src/__tests__/buildEmailHtml.test.ts` (or `buildDebugPayload.test.ts` if it exists, else add new test file) with cases that:
       - assert the new fields are in `LetterDebugPayload`
       - assert `buildEmailHtml` output contains "Wohin geht dein Brief?" only when routing-info is provided
       - assert no em-dash in the footer (`grep "—"` returns 0)

    Per `web/AGENTS.md`: read existing email-builder file structure first — patterns there must be preserved (inline styles, German font stack, no client-side JS).
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; (npx vitest run src/__tests__/buildEmailHtml.test.ts 2>/dev/null || npm test 2>/dev/null || true)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "routedPrimaryLevel\|routedPrimaryConfidence\|wasOverridden\|selectedLevel" web/src/lib/email/buildDebugPayload.ts` returns ≥ 4 (review D17 — all 4 new fields in debug payload).
    - `grep -c "Wohin geht dein Brief\|buildRoutingFooterHtml" web/src/lib/email/buildEmailHtml.ts` returns ≥ 2 (visible footer present).
    - `grep -c "—" web/src/lib/email/buildEmailHtml.ts` for any new edits stays at 0 (project rule).
    - `submitWizard.ts` populates the routing-info block when calling `sendLetterEmail`.
    - `buildEmailHtml` is graceful when routing-info is undefined (no crash, footer simply omitted or stripped).
    - `npx tsc --noEmit` passes.
    - All new tests pass.
  </acceptance_criteria>
  <done>Letter-Email shows a small "Wohin geht dein Brief?"-footer + the debug-URL payload now includes routing fields. Thomas can see at a glance in any sent email which level it went to and whether the user overrode the AI choice.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Read sample letters end-to-end + check email Routing-Footer</name>
  <what-built>
    Letter generator now branches per level. Bund letters argue structurally, Land letters reference state competence (often GG Art. 70), Gemeinde letters speak directly to local administration. The "stellvertretend" mode produces a noticeably different opening when a user picks the secondary recipient.
  </what-built>
  <how-to-verify>
    Run `cd web && npm run dev`. Submit six flows:

    1. **Bund:** PLZ anywhere + "Asylpolitik" → pick the MdB → read the letter. Expect: addressed as "Sehr geehrte Frau Abgeordnete [Name]" or "Sehr geehrter Herr Abgeordneter [Name]" (NO slash-form). Brief enthält bundesweites Framing, Verweis auf Bundesgesetzgebung oder Fraktions-/Ausschuss-Arbeit. **NO GG-Artikelnummern**.
    2. **Land:** PLZ in NRW + "Lehrermangel an meiner Schule" → pick the suggested MdL. Expect: addressed as "Sehr geehrter Herr Abgeordneter [Name]" (NO slash). Brief enthält Landeskompetenz-Framing, Verweis auf konkrete Landesinstrumente (z.B. Schulgesetz, Landesregierung), allgemeine Begriffe wie "Kulturhoheit" OK aber **NO Art. 70 GG**. Brief liest sich klar anders als der Bund-Brief aus Flow 1 (Strategie-Block wirkt, nicht nur Anrede-Swap).
    3. **Kommune (Generic-Brief — Thomas-Decision 2026-05-22):** PLZ in Köln (50667) + "Schlaglöcher in der Goethestraße" → Disambiguation zeigt "Stadtverwaltung Köln"-Card (synthetic recipient) + Override-Chips → pick the Stadtverwaltung. Expect:
       - Anrede ist **EXAKT** "Sehr geehrte Damen und Herren der Stadtverwaltung," — kein Bürgermeister-Name, kein "Frau/Herr [Titel]", kein Slash.
       - Brief enthält Kommune-Strategie-Block: lokale Direktheit, konkrete Verwaltungs-Einheit, weniger ideologisch.
       - Adressblock im Brief + Email: "Stadtverwaltung Köln, Stadt" + "50667 Köln, Stadt".
       - Success-Page zeigt RathausAdresseHint banner (PLAN 07) mit DuckDuckGo-Search-Link.
    4. **Stellvertretend-Flow:** Köln-Schlagloch wie Flow 3, aber User klickt "Land"-Chip in Disambiguation → wählt NRW-MdL. Expect: Brief mit Stellvertretend-Framing *"ich weiß, dass Sie als Landtagsabgeordnete für lokale Infrastruktur nicht direkt zuständig sind, diese liegt bei den Kommunen. Trotzdem wende ich mich an Sie, weil..."* (review B7, Thomas-Decision). User-Agency, nicht fiktive Kompetenz-Vorspiegelung.
    5. **Stadtstaat-Re-Route:** PLZ in Hamburg (20095) + "Krankenhausversorgung". Mistral routet Kommune (Krankenhausversorgung kann als kommunal gesehen werden), aber HH = Einheitsgemeinde → re-route auf Land. Erwartet: Senator/in-Card der Behörde (z.B. Sozialbehörde), Brief addressed as "Sehr geehrter Herr Senator [Name]" oder "Sehr geehrte Frau Senatorin [Name]".
    6. **Berlin-Bezirk:** PLZ in Berlin-Friedrichshain (10245) + "Spielplatz im Kiez ist kaputt" → Bezirksamt-Card erscheint. Expect:
       - Anrede "Sehr geehrte Damen und Herren des Bezirksamts,"
       - Adressblock "Bezirksamt Berlin, Friedrichshain-Kreuzberg" + PLZ.

    **Plus: prüfe die E-Mail nach jedem Flow:**
    7. **Email Routing-Footer (D17, alle Flows):** Öffne die abgesendete Email. Erwartung: kleiner grauer Footer "Wohin geht dein Brief? An die [Bundes/Landes/Kommunal]-Ebene ([Empfänger-Label]). KI-Vorauswahl: [...]-Ebene (Sicherheit: [...]). [Falls Override: 'Du hast die Ebene manuell auf [Level] gewechselt.']" — nicht zu prominent, aber sichtbar.
    8. **Debug-URL Payload (alle Flows):** Klick die Debug-URL am Ende der Email. JSON-Payload enthält `routedPrimaryLevel`, `routedPrimaryConfidence`, `wasOverridden`, `selectedLevel`.

    Acceptance: Thomas liest die 6 Briefe + 2 Email-Checks und bestätigt:
    - (a) **Framing materially unterschiedlich pro Ebene** — Bund-Brief vs. Land-Brief vs. Kommune-Brief lesen sich erkennbar anders (nicht nur Anrede)
    - (b) **NO GG-Artikelnummern** anywhere (`grep -c "Art\\.\\s*[0-9]\\+" <each-letter>` returns 0)
    - (c) Kommune-Anrede ist generic und korrekt
    - (d) Stellvertretend-Mode owns its competence-mismatch transparently
    - (e) Anrede ist gender-resolved für Bund/Land, generic für Kommune
    - (f) Routing-Footer in Email ist nachvollziehbar und nicht störend
    - (g) Debug-Payload ist vollständig (4 neue Felder)
    - (h) politisch credible — würde ein realer Empfänger das ernst nehmen?

    If any letter reads identical across levels OR contains "Art. X GG" OR Kommune uses non-generic Anrede OR email footer is missing → tune before merge.
  </how-to-verify>
  <resume-signal>Type "approved" or paste the problem letter so we can tune the prompt.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes.
- All tests green.
- No em dashes added.
- No new runtime npm dependency (D11).
</verification>

<success_criteria>
- A Bund letter, a Land letter, and a stellvertretend letter are visibly different.
- Existing letter quality (length, tone, German formality) is preserved.
- The wizard transparently passes through the level + mode without breaking the generic flow for users who don't see a secondary recommendation.
</success_criteria>

<output>
After completion, create `.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-06-SUMMARY.md`.
</output>
