# External Pre-Execute Review Prompt — Phase 999.6 (Brief nach Berlin)

> **For Thomas:** Copy the entire block below this line into a fresh agent session (Claude Code, Codex, etc.). The agent should NOT see this header. Paste from `## Mission` to the end of the file.

---

## Mission

You are doing a **pre-execute readiness review** of Phase 999.6 ("Multi-Level Politician Routing") for the German civic-tech app "Brief nach Berlin" (brief-nach-berlin.de). The phase has been through:

1. Initial planning (May 8 2026) — 6 PLAN files
2. Manual G1 routing-accuracy gate (May 21) — 100% unambiguous, 0 hard-fails
3. Multi-persona review (May 21) — 4 parallel agents (Data/API, AI/Prompt, UX, Constitutional-Law) surfaced 7 BLOCKER, 11 WARN, 4 NIT
4. Plan-file amendments (May 21) — all BLOCKER fixed, owner decided on the 4 design calls
5. Re-test G1 with 22 cases including injection + low-trigger (May 21) — 14/14 unambiguous, 2/2 low-trigger, 1/1 injection-neutralized
6. **Pivot 2026-05-22** — owner changed Kommune-strategy from Wikidata-Verweis to Generic-Rathaus-Brief. PLAN 07 was rewritten. PLAN 06 got per-level strategy-blocks + email routing-footer (D17). New context decisions D13 (rewritten) and D17 (new).

**Your job:** Final cross-file sanity check before executing the plan. Find anything inconsistent, contradictory, or still-broken that the prior review missed. **Pay particular attention to the 2026-05-22 pivot** — the Kommune-Generic-Rathaus-Brief flow is structurally different from the prior Verweis-Card plan, so cross-file consistency may have drifted.

**You must NOT:** edit any files, run any commands beyond `grep`/`Read`/`ls`, or execute the plan. Read-only review.

---

## Repository

Working directory: `/Users/thomas/Documents/Git Repos/brief-nach-berlin`

Stack: Next.js 16 (App Router) + TypeScript + Tailwind v4 + Mistral SDK + Abgeordnetenwatch API + OpenPLZ API. Hosted on Vercel Hobby.

Project rules that matter for this review:
- **No em-dashes** in user-facing copy (memory rule, replace with commas/colons)
- **German only** for UI + letter output (project is German civic-tech)
- **DSGVO**: no persistent user data, no DB, no auth in v1
- **Existing `PoliticalLevel` type** is `"Bund" | "Land" | "Kommune"` — note: **"Kommune" not "Gemeinde"** (this was the most expensive bug found in the prior review)

---

## Files to Read (in this order)

1. `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-REVIEW-CONSOLIDATED.md` — the prior review's full output (BLOCKER/WARN/NIT table + owner decisions)
2. `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-CONTEXT.md` — locked decisions D1–D22, Pre-Exec gates G1–G5
3. `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-04-level-router-PLAN.md` — Mistral routing call + lookupPLZ extension (the most critical plan)
4. `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-05-level-badge-ui-PLAN.md` — UI components (LevelBadge, LevelOverrideChips, EuComingSoonCard, disambiguation grouping)
5. `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-06-letter-prompt-level-adapt-PLAN.md` — letter generator with level + mode
6. `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-07-pragmatic-kommune-PLAN.md` — Generic-Rathaus-Brief with synthetic Stadtverwaltung/Bezirksamt recipient
7. `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-04-SUMMARY.md` — G1 + re-test results
8. (Reference only, do not deep-read) `/Users/thomas/Documents/Git Repos/brief-nach-berlin/.planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-RESEARCH.md` — 697 lines of background research
9. (Existing code to verify against) `/Users/thomas/Documents/Git Repos/brief-nach-berlin/web/src/lib/types/politician.ts` + `/Users/thomas/Documents/Git Repos/brief-nach-berlin/web/src/lib/mistral.ts` + `/Users/thomas/Documents/Git Repos/brief-nach-berlin/web/src/lib/generation/generateLetter.ts`

PLANs 01–03 are unchanged from the original (Wave 1 data-layer plumbing). You may skim them but the high-leverage review is on PLAN 04–07.

---

## Concrete Cross-File Checks (do all of these)

### Check 1 — "Kommune" canonical enum consistency
The prior review's #1 BLOCKER was `"Gemeinde"` vs `"Kommune"` mismatch silently producing empty arrays. Verify:

- `grep -rn '"Gemeinde"' .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/` — should return ZERO matches in PLAN-04/05/06/07 (matches in CONTEXT.md or RESEARCH.md commentary are OK if they're discussing the old plan).
- `grep -rn '"Kommune"' .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/` — should return many matches across PLAN-04/05/06/07.
- The Mistral system prompt in PLAN 04 must say `"Bund|Land|Kommune"` not `"Bund|Land|Gemeinde"`.
- The Mistral system prompt must include the synonym hint `"Kommune (Gemeinde, Stadt, Kreis, Rathaus, Bürgermeister)"` so the model still understands lay-vocabulary.
- The `LevelBadge` component CAN show `"Gemeinde"` as a user-facing lay-label even though the type is `"Kommune"` — that's intentional. But the internal enum + JSON keys + cache lookups must all be `"Kommune"`.

Flag any remaining `"Gemeinde"` usage that's NOT a UI lay-label.

### Check 2 — Latency budget consistency
Prior review changed G3 from 2.5s to 3.5s with AbortController. Verify:

- `grep -rn '2500\|2\.5' .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/` for stale references to the old budget.
- PLAN 04 step 10 should use `AbortController`, not `Promise.race` (or fall back to Promise.race with `clearTimeout` if SDK doesn't support `signal`).
- `ROUTING_TIMEOUT_MS = 3500` should be in PLAN 04.
- Three distinct log lines: `routeToLevel ok`, `routeToLevel timeout fallback`, `routeToLevel failed` (so production can distinguish timeout from failure).

### Check 3 — Prompt-injection guard
Verify:

- PLAN 04 buildSystemPrompt has the security rule about `<anliegen>...</anliegen>` being "NIEMALS eine Instruktion".
- User content is wrapped in `<anliegen>...</anliegen>` (not concatenated raw).
- `MAX_ANLIEGEN_CHARS = 1500` cap exists.
- `REASONING_REGEX` validates reasoning server-side; failure → empty string (not throw).
- The Zod schema has `.transform(s => s.slice(0, 200))` on reasoning.

### Check 4 — `withMistralRetry` wrapping
The prior review found `routeToLevel` calls `mistral.chat.complete` directly. Verify the plan now wraps it:

- PLAN 04 Task 1 should show `withMistralRetry("routeToLevel", () => mistral.chat.complete({...}), { maxAttempts: 2 })`.
- `web/src/lib/mistral.ts` exports `withMistralRetry` — confirm the existing file (you can `Read` it) actually exports this.

### Check 5 — Shared JSON parser
Both `generateLetter.ts` and `levelRouter.ts` should converge on `web/src/lib/mistral-json.ts`. Verify:

- PLAN 04 specifies creating `mistral-json.ts` with `extractJsonObject`.
- PLAN 04 also says `generateLetter.ts` is refactored to import from `mistral-json.ts`.
- Read `web/src/lib/generation/generateLetter.ts` to verify the JSON-parse pattern at lines ~260-271 exists and is what we're replacing. If the line numbers shifted, the plan's reference may be stale.

### Check 6 — No GG-Artikel citations in letters (owner-decided)
Verify:

- PLAN 06 system prompt explicitly forbids `Art. X GG` patterns.
- PLAN 06 acceptance criteria includes `grep -c "Art\\.\\s*[0-9]\\+\\s*GG" web/src/lib/generation/generateLetter.ts` returns 0.
- The Framing-Mapping (Land + primary) does NOT cite specific Article numbers anymore (allgemeine Begriffe wie "Kulturhoheit" sind OK).

### Check 7 — No-slash Anrede with gender + formal_title
Verify:

- PLAN 06 interfaces require `politician.gender` and `politician.formal_title` fields. Read `web/src/lib/types/politician.ts` and confirm these fields are not yet present — they need to be added during PLAN 06 execution (or PLAN 02 if Landtag-Fetcher should populate them from Abgeordnetenwatch).
- The Anrede mapping covers: gender=m/f → resolved form; gender=d → neutral form (e.g. "Sehr geehrte:r"); gender unknown → last-resort slash-form.
- Stadtstaaten (HH/HB/BE) special handling exists. HH/HB Kommune-Level rejected (Einheitsgemeinde). BE Kommune stays as Bezirksamt-style synthetic recipient, not a Wikidata person.

### Check 8 — Override chips above cards, not below
PLAN 05 Task 2 should render UI in this order: reasoning → override chips → primary group cards → secondary group cards → EU coming-soon card. Verify:

- PLAN 05's prose order matches.
- The Tailwind-rendered structure puts override chips ABOVE the cards (the prior plan had them below, which collided with the submit-button scroll).
- The `LevelOverrideChips` component spec exists (Task 2.5).
- "Zurück zur Empfehlung"-Link is in the component when override is active.

### Check 9 — Reasoning copy without lowercase-regex
The most subtle bug: the original PLAN 05 had `routingReasoning.replace(/^[A-ZÄÖÜ]/, c => c.toLowerCase())` which destroys German nouns ("weil bildungspolitik..." instead of "weil Bildungspolitik..."). Verify:

- PLAN 05 Task 2 step 3 does NOT call `.replace(/^[A-ZÄÖÜ]/, ...)`.
- PLAN 05 acceptance criteria: `grep -c "Empfohlen, weil\|replace.*toLowerCase" web/src/components/wizard/Step3Success.tsx` returns 0.
- The reasoning is rendered as a full sentence (Mistral returns "Bildungspolitik ist Ländersache." — display it as-is).
- No italic, no `text-warmgrau`, no `text-sm` for the reasoning — base text size, waldgruen-dark color.

### Check 10 — Coverage-hint copy locked + no em-dash + no false weiterleitung
The locked copy is:

> "[Bundesland] kommt bald dazu. Solange schreibst du an deine Bundestagsabgeordneten: Sie haben das Mandat, Themen aus allen Bundesländern in Berlin einzubringen."

Verify:

- `buildCoverageHint` in PLAN 04 Task 2 step 6 uses exactly this copy.
- No em-dash (`—`) anywhere in the PLANs.
- No "weitergeben" promise (Art. 38 Abs. 1 S. 2 GG: freies Mandat).
- There is no separate Kommune website-fallback path after the 2026-05-22 pivot; PLAN 07 injects a synthetic Stadtverwaltung / Bezirksamt recipient instead.

### Check 11 — Stellvertretend-Modus explicit competence-mismatch
Owner decided "always allow, frame transparently". Verify:

- PLAN 06 Task 1 step 5 has the template `"Ich weiß, dass Sie als Bundestagsabgeordnete für [Themenfeld] nicht direkt zuständig sind, diese liegt bei den Ländern."`
- No suppression at the Routing level (D6 still allows secondary for all combinations).
- PLAN 06 acceptance criteria: grep for `"nicht direkt zuständig"` returns ≥ 2.

### Check 12 — EU Coming-Soon-Card is static (no email capture)
Owner decided static placeholder, not interactive. Verify:

- PLAN 05 Task 2.6 `EuComingSoonCard` has `role="presentation"`, `aria-disabled="true"`, NO `onClick`, NO `<button>`, NO `<form>`, NO email input.
- Visual distinct from disabled-loading state (border-dashed, "Kommt bald"-Pill).
- Rendered once at the bottom of disambiguation, after `<hr>`.

### Check 13 — Pragmatic-Kommune (PLAN 07) flow
Verify:

- `lookupPLZWithLevel` injects a synthetic Kommune recipient into `byLevel.Kommune` instead of relying on a website lookup.
- There is no `gemeinde-websites.json`, no `resolveGemeindeVerweis(plz)`, and no runtime external dependency for Kommune.
- Step3Success.tsx keeps the normal politician-card flow for Kommune, just with a synthetic Stadtverwaltung/Bezirksamt card shape.
- Stadtstaat re-route: HH/HB Kommune → Land (Einheitsgemeinde Art. 28 Abs. 1 S. 2 GG). BE Kommune stays as a synthetic Bezirksamt recipient.

### Check 14 — G5 working-tree gate
The file `web/src/components/wizard/Step3Success.tsx` is M-modified (uncommitted). Verify:

- CONTEXT.md G5 documents this and requires `git diff` review before PLAN 05 Task 2 starts.
- PLAN 05 Task 2 has a pre-edit gate referencing G5.
- Run `git status web/src/components/wizard/Step3Success.tsx` and confirm it's still M-modified at review-time. If it's already committed/stashed, that's fine — note it.

### Check 15 — ROADMAP.md updated
Verify `.planning/ROADMAP.md` Phase 999.6 entry:
- References 7 plans (not 5).
- Mentions PLAN 07.
- The roadmap may still describe PLAN 07 as "Pragmatic-Verweis" — that's stale terminology. After the 2026-05-22 pivot, the correct description is "Generic-Rathaus-Brief mit synthetic Stadtverwaltung-Recipient". Note this as a WARN unless the file has been re-updated.

### Check 16 — Pivot 2026-05-22: Kommune is now a Generic-Rathaus-Brief (NEW)

The biggest change since the prior review. Verify:

- **CONTEXT.md D13** describes a Generic-Rathaus-Brief, NOT a Verweis-Card. The decision says:
  - Synthetic "Stadtverwaltung [Ortsname]" recipient injected into `byLevel.Kommune`
  - Generic Anrede `"Sehr geehrte Damen und Herren der Stadtverwaltung,"`
  - Postal address `"Stadtverwaltung [Ortsname]\n[PLZ] [Ortsname]"` (or `, ` separator depending on existing politicians-cache convention)
  - No Wikidata, no API fetch, no external dependency
  - DuckDuckGo-Suchhilfe via RathausAdresseHint banner on Success page
- **PLAN 07** is the rewritten version. Key signals it was correctly rewritten:
  - `grep -c "Wikidata\|P856\|SPARQL\|gemeinde-websites.json" .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-07-pragmatic-kommune-PLAN.md` returns **0** (no stale Wikidata references)
  - `grep -c "buildRathausRecipient\|RathausRecipient\|isSynthetic\|recipientKind" .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-07-pragmatic-kommune-PLAN.md` returns ≥ 5 (the new shape)
  - `grep -c "GemeindeVerweisCard" .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-07-pragmatic-kommune-PLAN.md` returns **0** (old component is gone)
- **PLAN 07 Stadtstaat handling:**
  - HH/HB throw `RathausRecipientNotApplicable` (Einheitsgemeinde)
  - BE uses `recipientKind: "bezirksamt"` with Anrede `"Sehr geehrte Damen und Herren des Bezirksamts,"`
- **RathausAdresseHint** is a Success-Page banner (not a Disambiguation-Card). It links to DuckDuckGo for the actual street address. User searches themselves, no Wikidata-Lookup.
- **PLAN 05 handles synthetic recipients:** in the card rendering, null `party` is conditionally skipped, null `abgeordnetenwatchUrl` already gracefully skipped (existing pattern). The Kommune-LevelBadge with Home-icon visually differentiates the synthetic card from real-politician cards.

If you find any reference to "GemeindeVerweisCard", "Wikidata P856", or "gemeinde-websites.json" in any PLAN file → that's a leftover from the pre-pivot plan and must be flagged as a BLOCKER (would cause confusion during execute).

### Check 17 — PLAN 06: Per-Level Strategic Framing Blocks (NEW)

Owner-decision 2026-05-22: the letter prompt must produce **materially different letters per level**, not just swap the Anrede. Verify:

- **PLAN 06 `interfaces` section** has a Framing-Mapping with EACH level having a distinct strategy block:
  - Bund: contains keywords "strukturelle" / "Fraktion" / "Ausschuss" / "Bundesgesetz"
  - Land: contains keywords "Landeskompetenz" / "Landesinstrumente" / "Landesgesetzgebung"
  - Kommune: contains keywords "Pflichtaufgaben" / "lokale Direktheit" / "Verwaltung" / "lösungs-orientiert"
- Each level has explicit:
  - **Strategie** (overall argumentation approach)
  - **Formalität** (formality level — Bund high, Land medium, Kommune lower-but-still-formal)
  - **Nachdruck-Hebel** (concrete persuasion levers for that level)
  - **Argumentations-Muster** (example sentence patterns)
  - **Verboten** (what to avoid for this level)
- **PLAN 06 Behavior Test 11** specifically tests that the three strategy blocks are materially different. If this test is missing or only tests Anrede-swap, that's a FAIL.
- **PLAN 06 acceptance criteria** has the grep for the strategy-block keywords.

### Check 18 — PLAN 06: Email Routing-Footer + Extended Debug Payload (D17, NEW)

Verify:

- **PLAN 06 Task 3** (NEW since 2026-05-22 pivot) exists and adds:
  - 4 new fields to `LetterDebugPayload`: `routedPrimaryLevel`, `routedPrimaryConfidence`, `routedSecondaryLevel`, `wasOverridden`, `selectedLevel` (5 total — confirm the actual count, doc may say 4-5).
  - A visible "Wohin geht dein Brief?" footer block in `buildEmailHtml.ts`
  - A `buildRoutingFooterHtml` helper function with unit tests
- **PLAN 06 `files_modified`** includes `web/src/lib/email/buildDebugPayload.ts`, `web/src/lib/email/buildEmailHtml.ts`, `web/src/lib/email/sendLetterEmail.ts`, `web/src/__tests__/buildEmailHtml.test.ts`.
- **PLAN 06 must_haves** mention D17 (Email-Footer + erweiterter Debug-URL-Payload).
- **CONTEXT.md D17** exists and describes both surfaces (visible footer + debug-URL-payload).
- The existing `web/src/lib/email/buildDebugPayload.ts` already has `politicalLevel` at line 35 — the new fields are additive, not replacing.

### Check 19 — Kommune Anrede is Generic (D13 pivot)

Verify in PLAN 06:

- `grep -c "Sehr geehrte Damen und Herren der Stadtverwaltung" .planning/phases/999.6-landtag-and-kommune-politician-coverage-expansion/999.6-06-letter-prompt-level-adapt-PLAN.md` returns ≥ 1
- Berlin variant `"Sehr geehrte Damen und Herren des Bezirksamts"` is also present
- Kommune-Anrede has NO gender-resolve (`gender=m/f/d`-branching does NOT apply to Kommune)
- Kommune-Anrede has NO formal_title-based personalization (no "Sehr geehrter Herr Oberbürgermeister X")
- Stale `"Oberbürgermeister"`-based Anrede examples should be **removed** from the Anrede-mapping (they remain valid for Stadtstaat-Land cases like "Senator" / "Erster Bürgermeister"). If any prose says "level=Kommune, gender=f, formal_title=Oberbürgermeisterin → ..." that's stale and a WARN.

---

## Owner-Decided Direction Calls (already settled, just verify the plans reflect them)

### 2026-05-21 decisions
| Question | Owner Answer | Should Be Reflected In |
|---|---|---|
| Stellvertretend-Modus for Land-exclusive topics | Always allow, frame transparently with "ich weiß, dass Sie nicht direkt zuständig sind" | PLAN 06 Task 1 step 5 |
| EU-MEPs Coming-Soon-Card | Static placeholder, no email capture | PLAN 05 Task 2.6 |
| GG-Artikel citations in letters | Completely forbidden in v1 | PLAN 06 + acceptance criteria |
| Re-test after fixes | Yes, with expanded set (22 cases) | `999.6-04-SUMMARY.md` |

### 2026-05-22 pivot (NEW — verify carefully)
| Question | Owner Answer | Should Be Reflected In |
|---|---|---|
| Kommune-Flow | **Generic-Rathaus-Brief** (no Wikidata, no Verweis-Card) — synthetic Stadtverwaltung recipient generates real letter | CONTEXT.md D13 (rewritten) + PLAN 07 (completely rewritten) + PLAN 06 (Kommune-Anrede generic) |
| Mistral-Prompt-Strategie | **Per-level strategic framing** (not just Anrede swap) — Bund=structural+fraction-leverage, Land=Landeskompetenz, Kommune=local directness+Pflichtaufgaben | PLAN 06 interfaces section + Test 11 + strategy-block-keyword greps |
| Email-Routing-Debug | **Visible footer + extended debug-URL-payload** with routedPrimary/routedSecondary/wasOverridden/selectedLevel | CONTEXT.md D17 (new) + PLAN 06 Task 3 (new) |

If you find any of these decisions NOT reflected in the actual plan files, that's a BLOCKER.

---

## What I want from you (output format)

A single Markdown response with these sections, nothing else:

### Section 1: Cross-Check Verdict (one line per check, 19 lines)
For each of Check 1–19, write:

```
- Check N — PASS / WARN / FAIL: <one-line summary>
```

If WARN/FAIL: include a specific file path + line/section reference in the summary.

Note: Checks 16–19 are NEW after the 2026-05-22 pivot. Prior reviewers won't have seen these — give them extra attention.

### Section 2: New Findings (only if you found anything the prior review missed)
For each new issue, write:
```
- [SEVERITY] <one-line title>
  File: <path>:<line>
  Issue: <2-3 sentences>
  Fix: <concrete recommendation>
```

Severity: BLOCKER (must fix before execute) / WARN (fix during execute) / NIT (track only).

If you find nothing new, write: "**No new findings beyond the prior consolidated review.**"

### Section 3: Ready-for-Execute Verdict (one paragraph)
End with one of:

- **"READY: proceed with /gsd-execute-phase 999.6"** — if no new BLOCKER + Check 14 (G5 working tree) is resolvable
- **"NOT READY: <reason>"** — if you found a new BLOCKER or a Check FAILed in a non-trivial way

Keep the entire response under 1500 words. No preamble, no acknowledgment of the task description, just the three sections.

---

## Tools you have

You should have access to: Read, Bash (for grep/ls only), Glob, Grep. You may NOT use: Edit, Write, NotebookEdit, or anything that mutates state. You may NOT use the Agent/Task tool to spawn sub-agents (this IS the sub-agent layer).

If you find yourself wanting to run the app or execute the plan: stop, that's outside scope. Just check that the plans are internally consistent and matched to the existing codebase shape.

---

Start now.
