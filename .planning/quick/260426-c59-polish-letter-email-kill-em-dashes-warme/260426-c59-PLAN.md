---
quick_id: 260426-c59
status: planned
mode: quick
created: 2026-04-26
description: Polish letter email + kill em dashes + warmer share copy + OG fix
---

# Quick Plan 260426-c59 — Polish letter email + kill em dashes + warmer share copy + OG fix

## Goal

Make the post-letter email feel like a personal note from the founder (warmer, less AI-product), eliminate em dashes from generated letters and templates, replace the share copy with a more natural founder-voice version, and fix the OG metadata to say "drei Minuten" and use the real `.de` domain.

## Context

Source plan (approved by user): `/Users/thomas/.claude/plans/please-check-the-letters-robust-ripple.md`

User feedback that drove this:
- Em dashes in letters and email feel like AI tells in German prose
- Email currently says "Die KI liefert den Rahmen" — too much AI framing for a warm civic tool
- Legal/disclaimer hints are stacked above the CTAs and feel too prominent
- Share copy ("Probier es selbst") is generic — should sound like the user wrote it themselves
- Page title still says "fünf Minuten" but the actual UX is "drei Minuten"
- OG image badge points at `brief-nach-berlin.vercel.app` instead of `brief-nach-berlin.de`
- Wants a personal sign-off in cursive ("Bleib dran. Thomas") at the bottom of the email
- Wants a vertical airmail stripe down the left edge of the email

## Tasks

### Task 1 — Kill em dashes in the letter generation prompt

**Files:**
- `web/src/lib/generation/generateLetter.ts`

**Action:**
- In the `REGELN` block of `SYSTEM_PROMPT_TEMPLATE` (around line 77), add a new bullet:
  > "Verwende KEINE Gedankenstriche (— oder –). Nutze stattdessen Kommata, Doppelpunkte, Klammern oder kurze Sätze."
- Replace every `—` em dash inside the `SYSTEM_PROMPT_TEMPLATE` string body (lines 36, 51, 57, 66, 71, 72) with commas, colons, or short rephrases. Models mirror the punctuation of their instructions, so cleaning the prompt is half the fix.
- Do NOT alter `LETTER_WRITING_KNOWLEDGE` (already em-dash-free) or `TONE_INSTRUCTIONS` content meaning — but if those have em dashes too, swap them.

**Verify:**
- `rg -n "—|–" web/src/lib/generation/generateLetter.ts` returns zero matches.
- File still compiles (run `npx tsc --noEmit -p web/tsconfig.json` in subsequent task or rely on next build step).

**Done when:** No em dashes remain in the prompt text and the new "no Gedankenstriche" rule is present in REGELN.

---

### Task 2 — Email template: design polish (vertical stripe, warm intro, copy hint, founder sign-off, reorder, em-dash cleanup, share-text consolidation)

**Files:**
- `web/src/lib/email/buildEmailHtml.ts`

**Action (do all of the following in one rewrite):**

**a) Vertical airmail stripe (left edge):**
Wrap the existing 600px content table in a 2-column outer table:
- Column 1: 8px wide, full table height. Outlook/Gmail fallback: solid `bgcolor="#C1121F"` (red). Modern clients: `style="background:repeating-linear-gradient(180deg,#C1121F 0 14px,#FAF8F5 14px 18px,#1D3557 18px 32px,#FAF8F5 32px 36px);"` matching the existing diagonal top stripe palette.
- Column 2: existing 600px-wide content table.
- Use `valign="top"` on both `<td>` cells so the stripe stretches the full content height.

**b) Rewrite intro paragraph (currently lines 78–84):**
Replace the existing italic intro with Thomas's exact warmer copy. Keep the existing `border-left:3px solid rgba(45,80,22,0.45);padding-left:16px;` quote-style wrapper. New copy:
> "Hier ist dein Briefentwurf. Lies ihn gründlich durch, bevor du ihn abschreibst, und pass ihn an, damit es dein Brief wird. Formulierungen, Ton, einzelne Argumente: alles darf von dir kommen. Wir helfen dir hoffentlich, damit dein Anliegen schneller auf dem richtigen Schreibtisch landet. Danke, dass du dich engagierst."

No em dashes. No "KI" mention.

**c) Subtle copy hint below the letter block (insert after the letter `<div>` on line 90, still within the same `<td>`):**
Add a small italic fine-print line, NOT a button:
```html
<p style="margin:8px 0 0;font-family:Georgia,'Times New Roman',serif;font-size:11px;color:#999999;font-style:italic;line-height:1.5;">
  Lieber per Hand abschreiben. Aber wenn du den Text zwischendurch in Word brauchst: ins Feld klicken, Cmd/Strg + A, dann Cmd/Strg + C.
</p>
```

**d) Add Caveat web font in `<head>`:**
After the `<meta name="viewport">` tag, add:
```html
<link href="https://fonts.googleapis.com/css2?family=Caveat:wght@500&display=swap" rel="stylesheet">
```
Apple Mail / iOS Mail will load this; Outlook/Gmail web ignore it and fall back gracefully via the font stack on the signature element.

**e) Personal sign-off from Thomas (NEW row, inserted between the CTAs row (currently 172–196) and the Footer row (currently 198–205)):**
```html
<tr>
  <td colspan="7" style="padding:0 32px 24px;background-color:#ffffff;text-align:left;">
    <p style="margin:0 0 4px;font-family:Georgia,'Times New Roman',serif;font-size:14px;color:#4A4A4A;line-height:1.6;">
      Vielen Dank, dass du schreibst.
    </p>
    <p style="margin:0;font-family:'Caveat','Brush Script MT','Lucida Handwriting',cursive;font-size:32px;color:#1D3557;line-height:1.1;">
      Thomas
    </p>
  </td>
</tr>
```

**f) Reorder bottom of email — disclaimer goes BELOW the footer:**
- Remove the existing Disclaimer row (currently 154–170).
- Keep CTAs row (172–196) where it is.
- Keep the new Personal Sign-off row immediately after CTAs (from step 2e).
- Keep the Footer row (198–205) as-is, but soften: change "Brief nach Berlin , Deine Stimme zählt." (after em-dash removal) to read cleanly with a comma or middle dot.
- After the footer row, add a NEW Legal/disclaimer row, smaller/lighter. Use `font-size:11px; color:#aaaaaa;`:
```html
<tr>
  <td colspan="7" style="padding:8px 32px 24px;background-color:#FAF8F5;text-align:center;">
    <p style="margin:0 0 6px;font-family:Georgia,'Times New Roman',serif;font-size:11px;color:#aaaaaa;line-height:1.5;">
      <strong>Hinweis:</strong> Dieser Brief ist ein generierter Entwurf, keine Meinungsäußerung von Brief nach Berlin. Wir empfehlen, ihn an deinen eigenen Stil und deine persönliche Haltung anzupassen, bevor du ihn verschickst. Die Verantwortung für den Inhalt liegt bei dir.
    </p>
    <p style="margin:0 0 6px;font-family:Georgia,'Times New Roman',serif;font-size:11px;color:#aaaaaa;line-height:1.5;">
      Bitte prüfe Politikerdaten und Brieftext vor dem Versand anhand offizieller Quellen (<a href="https://www.bundestag.de" style="color:#888888;">bundestag.de</a>, Landtags- oder Rathauswebsite).
    </p>
    <p style="margin:0;font-family:Georgia,'Times New Roman',serif;font-size:11px;color:#aaaaaa;line-height:1.5;">
      <a href="https://brief-nach-berlin.de/datenschutz" style="color:#888888;">Datenschutzerklärung</a>: deine Daten werden nach Versand dieser E-Mail nicht gespeichert.
    </p>
  </td>
</tr>
```

Drop the "KI-" prefix from the disclaimer ("KI-generierter Entwurf" → "generierter Entwurf"). Drop the standalone "KI kann Fehler machen" lead-in (folded into the body of the prüfe-Politikerdaten line above).

**g) Strip every remaining `&mdash;` and `—` from the template HTML:**
Currently at lines 81 (now replaced), 117, 159 (now replaced), 166 (now replaced), 202. After steps b/f the only remaining em dashes should be on lines 117 ("Sie können den Brief auch ausdrucken — das ist immer noch besser als gar nichts.") and 202 (footer "Brief nach Berlin — Deine Stimme zählt."). Replace each with a comma, colon, or rephrase:
- Line 117: "Sie können den Brief auch ausdrucken. Das ist immer noch besser als gar nichts."
- Line 202: "Brief nach Berlin · Deine Stimme zählt." (middle dot reads as a tasteful brand divider).

Final grep `rg -n '—|&mdash;' web/src/lib/email/buildEmailHtml.ts` must return zero matches.

**h) Import SHARE_TEXT from config (replace the duplicate):**
Currently lines 38–40 hardcode the share text. Replace with:
```ts
import { APP_NAME, APP_URL, WIZARD_PATH, WIZARD_TEXT_PARAM_MAX_LENGTH, SHARE_TEXT } from "@/lib/config";
// ...
const shareText = encodeURIComponent(SHARE_TEXT);
```
Single source of truth lives in `config.ts` (Task 3 below replaces its content).

**Verify:**
- `rg -n '—|&mdash;|KI-generiert' web/src/lib/email/buildEmailHtml.ts` returns zero matches.
- `rg -n 'Ich habe gerade einen Brief' web/src/lib/email/buildEmailHtml.ts` returns zero matches (hardcoded share text fully removed).
- File compiles via TypeScript (covered by Task 6 build step).
- Manual eye-check of the rendered HTML structure: outer 2-col table with stripe, intro, letter, copy-hint, address, next steps, CTAs, signature, footer, legal block in that order.

**Done when:** All eight sub-changes (a–h) are applied, no em dashes remain, SHARE_TEXT is imported from config, and the new bottom order is CTAs → signature → footer → legal.

---

### Task 3 — Update SHARE_TEXT to founder-voice copy

**Files:**
- `web/src/lib/config.ts`

**Action:**
Replace the current `SHARE_TEXT` definition (lines 23–25) with:
```ts
export const SHARE_TEXT = `Hey, ich hab eben meinem Abgeordneten in Berlin geschrieben und fand's hiermit total einfach. Wenn du dir auch mal Luft verschaffen willst, kann ich Brief nach Berlin nur empfehlen, ist komplett kostenlos: ${APP_URL}` as const;
```

Keep `SHARE_URL_WHATSAPP` and `SHARE_URL_TWITTER` as-is (they reference `SHARE_TEXT` and will pick up the new copy automatically).

**Verify:**
- `rg -n 'Probier es selbst' web/src/lib/config.ts` returns zero matches.
- The new copy is present and uses backticks + `${APP_URL}` interpolation.
- Stays under 280 chars: `node -e "console.log(\`Hey, ich hab eben meinem Abgeordneten in Berlin geschrieben und fand's hiermit total einfach. Wenn du dir auch mal Luft verschaffen willst, kann ich Brief nach Berlin nur empfehlen, ist komplett kostenlos: https://brief-nach-berlin.de\`.length)"` should print a number under 280.

**Done when:** `SHARE_TEXT` exports the new founder-voice copy and the X/WhatsApp URLs derive from it without modification.

---

### Task 4 — Fix OG title and URL in layout.tsx

**Files:**
- `web/src/app/layout.tsx`

**Action:**
- Change `metadataBase: new URL("https://brief-nach-berlin.vercel.app")` → `metadataBase: new URL("https://brief-nach-berlin.de")` (line 22).
- Change all three occurrences of `"in fünf Minuten"` → `"in drei Minuten"` (lines 23, 27, 36) — that's the `title`, `openGraph.title`, and `twitter.title` strings.
- Change `openGraph.url: "https://brief-nach-berlin.vercel.app"` → `"https://brief-nach-berlin.de"` (line 32).
- `description` text doesn't mention any minute count — leave it alone.

**Verify:**
- `rg -n 'fünf Minuten|vercel\.app' web/src/app/layout.tsx` returns zero matches.
- `rg -n 'drei Minuten|brief-nach-berlin\.de' web/src/app/layout.tsx` returns the expected updated lines.

**Done when:** Layout metadata uses "drei Minuten" and `brief-nach-berlin.de` everywhere.

---

### Task 5 — Fix OG image URL badge

**Files:**
- `web/src/app/opengraph-image.tsx`

**Action:**
Change line 132 — the URL badge text inside the `<div>` rendered into the OG image — from `brief-nach-berlin.vercel.app` to `brief-nach-berlin.de`.

**Verify:**
- `rg -n 'vercel\.app' web/src/app/opengraph-image.tsx` returns zero matches.
- After deploy, the rendered OG image badge shows `brief-nach-berlin.de`.

**Done when:** The URL badge in the dynamically generated OG image displays the production domain.

---

### Task 6 — Build + grep verification

**Files:** none (verification only)

**Action:**
1. From `/Users/thomas/Documents/Git Repos/brief-nach-berlin/web`, run `npm run build` (or `npx next build`) and confirm it completes without TypeScript or build errors.
2. Run a final em-dash sweep across all changed files:
   ```bash
   rg -n '—|&mdash;' web/src/lib/email/buildEmailHtml.ts web/src/lib/generation/generateLetter.ts
   ```
   Expected: zero matches.
3. Run a final stale-domain sweep:
   ```bash
   rg -n 'brief-nach-berlin\.vercel\.app' web/src/
   ```
   Expected: zero matches in the changed files (other places may legitimately still reference Vercel for deploy; only the OG/share/email surfaces are in scope).

**Done when:** Build succeeds and both grep sweeps confirm clean output for the changed files.

---

## Must-haves (verifier checklist)

- [ ] No em dashes (`—` or `&mdash;`) in `buildEmailHtml.ts` or in the system-prompt body of `generateLetter.ts`.
- [ ] `SYSTEM_PROMPT_TEMPLATE` includes the new "Verwende KEINE Gedankenstriche" rule in its REGELN block.
- [ ] `buildEmailHtml.ts` has a 2-column outer table with a vertical stripe column (8px wide, red fallback + linear-gradient).
- [ ] Email intro paragraph uses Thomas's warm copy verbatim, with no "KI" mention.
- [ ] A subtle italic copy-hint exists directly below the letter block.
- [ ] A handwritten "Thomas" signature row exists between the CTAs and the footer, using a Caveat / Brush Script MT font stack.
- [ ] The Google Fonts `<link>` for Caveat is present in the email `<head>`.
- [ ] The legal/disclaimer block sits BELOW the footer, smaller (font-size 11px), and no longer says "KI-generiert".
- [ ] `buildEmailHtml.ts` imports `SHARE_TEXT` from `@/lib/config` instead of hardcoding it.
- [ ] `config.ts` exports the new founder-voice `SHARE_TEXT`.
- [ ] `layout.tsx` says "drei Minuten" everywhere and uses `brief-nach-berlin.de` for `metadataBase` and `openGraph.url`.
- [ ] `opengraph-image.tsx` URL badge says `brief-nach-berlin.de`.
- [ ] `npm run build` (in `web/`) completes successfully.

## Out of scope (explicitly NOT changing)

- `Step3Success.tsx` — user said copy lives in the email only as a subtle hint; no on-screen letter display, no copy button on the success page.
- OG image artwork — only the URL badge text changes; the design is fine.
- Per-share dynamic OG images — out of scope (link previews are platform-cached; the share text is the real personalization lever).
- Datenschutz page content, abgeordnetenwatch logic, wizard flow — none of those.
