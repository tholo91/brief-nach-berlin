---
phase: 260521-tn1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/feedback/feedbackTags.ts
  - web/src/lib/generation/generateLetter.ts
autonomous: true
requirements:
  - REVIEW-CHIPS — 3 neue Negativ-Chips (details_erfunden, anliegen_verfehlt, wiederholt_sich)
  - ANTI-HALLU — positive Vorgabe "ÜBER DEN ABSENDER" im System-Prompt + Kurz-Input-Hinweis (<200 chars) im User-Prompt
  - PROMPT-FIX — Pflicht-Element 2 "KOMPLEXITÄT ANERKENNEN" streichen + Punkte renumberieren
  - LENGTH-KORRIDOR — Retry-Schwelle auf ±15% Akzeptanz-Korridor verschieben, wordCountInRange-Telemetrie unverändert
must_haves:
  truths:
    - "User sieht 3 neue Negativ-Chips im Feedback-Formular bei 1-3-Sterne-Bewertungen"
    - "Bei kurzem Input (<200 Zeichen) erscheint im User-Prompt ein <hinweis>-Block, der vor erfundener Vita warnt"
    - "Der System-Prompt enthält den Block 'ÜBER DEN ABSENDER (positive Vorgabe)' direkt nach KEINE-ERFINDUNGEN"
    - "Pflicht-Element 'KOMPLEXITÄT ANERKENNEN' ist aus dem Prompt entfernt, nachfolgende Punkte sind renumberiert (1. ANLASS, 2. EINE BITTE, 3. SCHLUSS-SATZ)"
    - "Wortzahl innerhalb ±15% des Min/Max-Fensters triggert keinen Retry mehr"
    - "wordCountInRange-Flag im debug_payload bleibt am ursprünglichen Min/Max ausgerichtet"
  artifacts:
    - path: "web/src/lib/feedback/feedbackTags.ts"
      provides: "Allowlist mit 9 Negativ-Chips (6 alte + 3 neue), 4 Positiv-Chips"
      contains: "details_erfunden"
    - path: "web/src/lib/generation/generateLetter.ts"
      provides: "Angepasster SYSTEM_PROMPT + buildUserPrompt + Retry-Logik mit Akzeptanz-Korridor"
      contains: "acceptableMin"
  key_links:
    - from: "web/src/lib/feedback/feedbackTags.ts"
      to: "web/src/lib/actions/submitReview.ts (z.enum(FEEDBACK_TAG_SLUGS))"
      via: "Re-export der FEEDBACK_TAG_SLUGS Konstante"
      pattern: "FEEDBACK_TAG_SLUGS"
    - from: "web/src/lib/generation/generateLetter.ts (buildUserPrompt)"
      to: "Mistral chat.complete user message"
      via: "input.issueText.length < 200 → <hinweis>-Block prepended"
      pattern: "issueText.length"
    - from: "web/src/lib/generation/generateLetter.ts (generateLetter)"
      to: "Retry-Branch (mistral.chat.complete:length-retry)"
      via: "wordCount < acceptableMin || wordCount > acceptableMax"
      pattern: "acceptableMin|acceptableMax"
---

<objective>
Review-Lernschleife aus 4 echten Reviews (21. Mai 2026): Halluzinations-Risiko senken (Vita erfunden), Wiederholungs-Bug "Ich verstehe, dass…" beheben (selbstverschuldetes Pflicht-Element streichen), Latenz senken (Retry-Korridor ±15%), präziseres Feedback einsammeln (3 neue Chips).

Purpose: Aus echtem User-Feedback lernen statt blind weiter zu polishen. Jede Änderung adressiert ein konkretes Problem aus den Reviews 2/3/4. Mistral-/Anthropic-Docs empfehlen positive Vorgaben statt Verbotslisten — daher punktuelle positive Ergänzung + Streichen des Bug-erzeugenden Pflicht-Elements, KEINE neuen Verbotslisten.

Output: 2 modifizierte Dateien, 0 neue Dependencies, 0 DB-Migrationen, 0 UI-Komponenten-Änderungen. Sofort deploybar nach grünem `tsc --noEmit` + `npm run lint`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./web/AGENTS.md
@.planning/STATE.md
@./web/src/lib/feedback/feedbackTags.ts
@./web/src/lib/generation/generateLetter.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-21. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From web/src/lib/feedback/feedbackTags.ts:
```typescript
export const NEGATIVE_FEEDBACK_TAGS = [
  { slug: "zu_lang", label: "Zu lang" },
  { slug: "zu_kurz", label: "Zu kurz" },
  { slug: "falscher_ton", label: "Falscher Ton" },
  { slug: "zu_generisch", label: "Zu generisch" },
  { slug: "klingt_nicht_nach_mir", label: "Klingt nicht nach mir" },
  { slug: "mdb_passt_nicht", label: "MdB passt nicht" },
] as const;

export const POSITIVE_FEEDBACK_TAGS = [ /* unchanged */ ] as const;

export type FeedbackTagSlug =
  | (typeof NEGATIVE_FEEDBACK_TAGS)[number]["slug"]
  | (typeof POSITIVE_FEEDBACK_TAGS)[number]["slug"];

export const FEEDBACK_TAG_SLUGS: readonly FeedbackTagSlug[] = [
  ...NEGATIVE_FEEDBACK_TAGS.map((t) => t.slug),
  ...POSITIVE_FEEDBACK_TAGS.map((t) => t.slug),
];
```

From web/src/lib/generation/generateLetter.ts (current relevant locations):
- Line 84: `REGEL — KEINE ERFINDUNGEN (nicht verhandelbar): …` — neuer Block "ÜBER DEN ABSENDER" wird direkt darunter eingefügt
- Lines 147-151: `PFLICHT-ELEMENTE:` mit 4 Punkten — Punkt 2 (KOMPLEXITÄT ANERKENNEN) komplett entfernen, 3 und 4 zu 2 und 3 hochziehen
- Lines 203-237: `buildUserPrompt(input, minWords, maxWords, toneLevel)` — vor `<transkript>`-Block conditional `<hinweis>` prepend, wenn `input.issueText.length < 200`
- Lines 263-270: `generateLetter`-Funktion, Konstanten `minWords`, `maxWords`, `targetWords`, `maxTokens` — direkt danach `acceptableMin` und `acceptableMax` berechnen
- Line 296: `if (wordCount < minWords || wordCount > maxWords)` → ersetzen durch `if (wordCount < acceptableMin || wordCount > acceptableMax)`
- Line 346: `const wordCountInRange = wordCount >= minWords && wordCount <= maxWords;` — UNVERÄNDERT lassen (Telemetrie am Ziel-Fenster, nicht am Korridor)

Server-Validation in web/src/lib/actions/submitReview.ts:
```typescript
z.enum(FEEDBACK_TAG_SLUGS as [string, ...string[]])
```
→ Neue Slugs werden automatisch akzeptiert, sobald sie in NEGATIVE_FEEDBACK_TAGS stehen. Keine Action-Änderung nötig.

Form-Rendering in web/src/app/(site)/feedback/FeedbackForm.tsx:142:
Form iteriert über NEGATIVE_FEEDBACK_TAGS / POSITIVE_FEEDBACK_TAGS und rendert Chips dynamisch. Keine UI-Änderung nötig.

DB-Spalte `reviews.feedback_tags` ist `text[]` → keine Migration.
</interfaces>

<background>
Approved plan source: `~/.claude/plans/a-nimm-nur-folgende-parallel-teacup.md`. Plan basiert auf Analyse von `~/Downloads/Reviews Tabelle von Thomas Lorenz.csv` (4 echte Reviews):
- Review 1 (Anna, 5★): unverändert positiv
- Review 2 (Jonathan, 2★): Vita erfunden ("als Lehrer seit 15 Jahren" — stand nicht im Input) → Task 2 fixt das
- Review 3 (Florian, 3★): Wiederholungen "Ich verstehe, dass…" + Logik-Flip bei GEG → Task 2 fixt Wiederholungs-Bug, Logik-Flip-Schutz NICHT in diesem Scope
- Review 4 (anonym, 2★): kein Body, separat per Mail nachfassen

Telemetrie-Befund: 2 von 4 Briefen landeten außerhalb des Ziel-Fensters (4.6% bzw. 6.1% daneben), triggerten teure Retries (19-40s Latenz), die oft als "nicht näher am Ziel" verworfen wurden. ±15%-Korridor spart ~10s in ~50% der Fälle.

Not in scope (bewusst): Stance-Detection, Web-Search via Mistral Agents, Landtag/Kommune-Politiker, Fine-Tuning, neue Verbotslisten.
</background>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 neue Negativ-Chips in feedbackTags.ts ergänzen</name>
  <files>web/src/lib/feedback/feedbackTags.ts</files>
  <action>
Erweitere `NEGATIVE_FEEDBACK_TAGS` um drei neue Einträge am Ende des Arrays (nach `mdb_passt_nicht`):

```typescript
{ slug: "details_erfunden", label: "Details über mich erfunden" },
{ slug: "anliegen_verfehlt", label: "Anliegen falsch verstanden" },
{ slug: "wiederholt_sich", label: "Wiederholt sich" },
```

`POSITIVE_FEEDBACK_TAGS`, der `FeedbackTagSlug`-Type-Union und das `FEEDBACK_TAG_SLUGS`-Re-Export bleiben unverändert — die `as const`-Assertion propagiert die neuen Slugs automatisch in den Union-Type und damit in `z.enum(FEEDBACK_TAG_SLUGS)` der Server-Action.

KEINE UI-Änderung an `FeedbackForm.tsx` nötig (rendert dynamisch). KEINE DB-Migration nötig (`feedback_tags` ist `text[]`).

Reihenfolge im Array ist sichtbarkeitsrelevant — neue Chips ans Ende, damit etablierte Negativ-Chips oben bleiben.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "feedbackTags|submitReview|FeedbackForm" || echo "OK: no type errors in affected files"</automated>
  </verify>
  <done>
- `NEGATIVE_FEEDBACK_TAGS` enthält 9 Einträge (6 alt + 3 neu in genau dieser Reihenfolge: details_erfunden, anliegen_verfehlt, wiederholt_sich)
- `npx tsc --noEmit` läuft fehlerfrei durch
- `grep -c "details_erfunden\|anliegen_verfehlt\|wiederholt_sich" web/src/lib/feedback/feedbackTags.ts` gibt `3`
  </done>
</task>

<task type="auto">
  <name>Task 2: Anti-Halluzinations-Vorgabe + Kurz-Input-Hinweis + Pflicht-Element streichen</name>
  <files>web/src/lib/generation/generateLetter.ts</files>
  <action>
Drei zusammenhängende Prompt-Änderungen in `web/src/lib/generation/generateLetter.ts`. Reihenfolge der Edits ist egal, aber alle drei MÜSSEN in einem zusammenhängenden Edit-Block landen, damit der Prompt konsistent bleibt.

**(A) Positive Anti-Halluzinations-Vorgabe im System-Prompt:**

In `SYSTEM_PROMPT_TEMPLATE` (ab Zeile 74), DIREKT nach dem bestehenden Block "REGEL — KEINE ERFINDUNGEN (nicht verhandelbar): …" (endet aktuell auf Zeile 85), eine Leerzeile, dann diesen Block einfügen:

```
ÜBER DEN ABSENDER (positive Vorgabe):
Beschreibe den Absender ausschließlich mit Informationen, die wörtlich in <transkript> oder <absender_optional> stehen. Wenn der Bürger sich selbst nicht beschreibt, schreibe in allgemeiner Ich-Form ohne Vita, ohne Berufsangabe, ohne Familiensituation, ohne Wohnhistorie, ohne politische Vorgeschichte. Lieber abstrakt-allgemein als plausibel klingende Erfindung.
```

Den Block "REGEL — STIMME DES BÜRGERS BEWAHREN" und alles danach UNVERÄNDERT lassen.

**(B) Pflicht-Element "KOMPLEXITÄT ANERKENNEN" streichen + renumberieren:**

Im selben Template, im Abschnitt `PFLICHT-ELEMENTE:` (aktuell Zeilen 147-151):

ENTFERNE komplett:
```
2. KOMPLEXITÄT ANERKENNEN: ein Satz, der zeigt, dass du verstehst, warum das Thema schwierig ist. Maximal eine Zahl oder ein Fakt aus <transkript>, nicht mehr.
```

ÄNDERE Punkt 3 zu Punkt 2 (Wortlaut unverändert):
```
2. EINE BITTE: genau ein konkretes Verb plus ein konkretes Objekt. Keine Aufzählung, keine Wunschliste. Nicht "ich bitte um Maßnahmen", sondern z. B. "ich bitte Sie, sich im Verkehrsausschuss für die Aufnahme dieser Strecke ins Sonderprogramm einzusetzen".
```

ÄNDERE Punkt 4 zu Punkt 3 (Wortlaut unverändert):
```
3. SCHLUSS-SATZ vor Grußformel: eine politische Handlungserwartung an die Empfängerin/den Empfänger. KEINE Bitte um Antwort, KEIN Gesprächs- oder Treffen-Wunsch, KEIN "ich freue mich auf Ihre Rückmeldung". Das Ziel ist politische Wirkung, nicht Korrespondenz.
```

Punkt 1 (KONKRETER ANLASS) bleibt UNVERÄNDERT als Punkt 1. Kein Ersatz, keine neue Verbotsliste für "Ich verstehe, dass…" — das Pflicht-Element entfällt ersatzlos.

**(C) Kurz-Input-Hinweis in `buildUserPrompt`:**

In `buildUserPrompt` (ab Zeile 203): VOR dem Return-Statement, conditional einen `hinweis`-String berechnen, der nur dann gesetzt wird, wenn der Bürger sich knapp gehalten hat.

Implementierung:

```typescript
const kurzHinweis = input.issueText.length < 200
  ? `<hinweis>
Der Bürger hat sich knapp gehalten. Bleibe entsprechend abstrakt: keine Erlebnis-Szenen, keine Vita, keine konkreten Ortsangaben außer Stadt/Stadtteil, wenn vorhanden.
</hinweis>

`
  : "";
```

Dann im Return-Template `${kurzHinweis}` DIREKT VOR `<transkript>` einfügen:

```typescript
return `${kurzHinweis}<transkript>
${input.issueText}
</transkript>
…rest unverändert…`;
```

Schwellwert 200 Zeichen (NICHT Wörter) — passt zum CSV-Befund, dass Jonathans Input kurz war und Mistral trotzdem Vita ausgeschmückt hat. Wenn `issueText.length >= 200`, ist `kurzHinweis` ein leerer String und der Prompt sieht aus wie heute.

**KEINE Änderungen** an: `tonalityBlock`, `mdbContextBlock`, `absenderBlock`, der "VERBOTENE WÖRTER"-Liste, der "VERBOTENE STRUKTUREN"-Liste, "REGELN"-Block am Ende, JSON-Response-Schema. Wir erweitern NUR um eine positive Vorgabe + einen conditional Hinweis und streichen EIN Pflicht-Element.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && grep -c "ÜBER DEN ABSENDER" src/lib/generation/generateLetter.ts && grep -c "KOMPLEXITÄT ANERKENNEN" src/lib/generation/generateLetter.ts</automated>
  </verify>
  <done>
- `tsc --noEmit` läuft fehlerfrei
- `grep -c "ÜBER DEN ABSENDER" web/src/lib/generation/generateLetter.ts` gibt `1`
- `grep -c "KOMPLEXITÄT ANERKENNEN" web/src/lib/generation/generateLetter.ts` gibt `0`
- `grep -n "PFLICHT-ELEMENTE" -A 8 web/src/lib/generation/generateLetter.ts` zeigt genau 3 nummerierte Punkte (1. KONKRETER ANLASS, 2. EINE BITTE, 3. SCHLUSS-SATZ)
- `grep -c "kurzHinweis\|<hinweis>" web/src/lib/generation/generateLetter.ts` ist >= 2 (ein definition, mindestens ein usage / oder eines im Template-String)
- Lokal kurzer Test-Input (<200 Zeichen) im Wizard erzeugt einen Brief ohne erfundene Vita-Floskeln (manuelle Stichprobe — siehe verification section)
  </done>
</task>

<task type="auto">
  <name>Task 3: ±15%-Akzeptanz-Korridor für Retry-Entscheidung</name>
  <files>web/src/lib/generation/generateLetter.ts</files>
  <action>
Logik-Änderung in der `generateLetter`-Funktion (ab Zeile 263).

**Schritt 1: Konstanten ergänzen** direkt nach `maxTokens` (Zeile 270), vor `systemPrompt`-Berechnung:

```typescript
// Akzeptanz-Korridor: Retry nur, wenn Wortzahl außerhalb ±15% um das Ziel-Fenster liegt.
// Reduziert teure Retries auf Briefe, die ohnehin meist verworfen werden (s. Telemetrie 21.05.).
// wordCountInRange (unten, für debug_payload) bleibt am ursprünglichen Min/Max ausgerichtet,
// damit die Telemetrie weiter zeigt, wie oft Mistral das Ziel trifft (nicht nur den Korridor).
const acceptableMin = Math.floor(minWords * 0.85);
const acceptableMax = Math.ceil(maxWords * 1.15);
```

**Schritt 2: Retry-Bedingung** auf Zeile 296 ersetzen:

VORHER:
```typescript
if (wordCount < minWords || wordCount > maxWords) {
```

NACHHER:
```typescript
if (wordCount < acceptableMin || wordCount > acceptableMax) {
```

**Schritt 3: console.warn-Payload** beim Retry-Trigger erweitern (innerhalb des if-Blocks, ab Zeile 302), damit die Telemetrie sichtbar macht, dass jetzt der Korridor entscheidet:

```typescript
console.warn("[generateLetter] word count out of range, retrying once", {
  wordCount,
  minWords,
  maxWords,
  acceptableMin,
  acceptableMax,
  lengthKey,
  direction: wordCount < acceptableMin ? "too_short" : "too_long",
});
```

(direction-Check ebenfalls auf `acceptableMin` umstellen.)

**Schritt 4: Retry-Direktive an Mistral** (Zeile 298-300) UNVERÄNDERT lassen — die kommuniziert weiter das Ziel-Fenster `${minWords}–${maxWords}`, NICHT den Korridor. Wir machen Mistral nicht weicher, wir senken nur unsere Schwelle für teure Korrektur-Calls.

**Schritt 5: `wordCountInRange`-Flag** auf Zeile 346 UNVERÄNDERT lassen:

```typescript
const wordCountInRange = wordCount >= minWords && wordCount <= maxWords;
```

Telemetrie-Begründung: Wenn wir hier auch den Korridor verwenden würden, könnten wir nie sehen, ob das Ziel-Fenster zu eng kalibriert ist oder Mistral systematisch daneben liegt. `wordCountInRange` ist das wahre Qualitäts-Signal, `acceptableMin/Max` ist nur die Retry-Schwelle.

**KEINE Änderungen** an: `maxTokens` (bleibt großzügig, ~778 Tokens bei 240-Wörter-Ziel), `targetWords`, der "Only accept the retry if closer to target"-Logik (Zeilen 329-340), `LETTER_LENGTHS`-Definition, `<ziel>`-Block im User-Prompt.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && grep -c "acceptableMin\|acceptableMax" src/lib/generation/generateLetter.ts</automated>
  </verify>
  <done>
- `tsc --noEmit` läuft fehlerfrei
- `grep -c "acceptableMin" web/src/lib/generation/generateLetter.ts` gibt mindestens `3` (1 Definition + Retry-Bedingung + console.warn-Payload + direction-Check — je nach Schreibweise 3-4)
- `grep -c "acceptableMax" web/src/lib/generation/generateLetter.ts` gibt mindestens `2`
- `grep -n "const wordCountInRange" web/src/lib/generation/generateLetter.ts` zeigt die Zeile UNVERÄNDERT mit `>= minWords && wordCount <= maxWords`
- Manuelle Trace-Prüfung: Wortzahl `minWords - 5%` (innerhalb Korridor) → KEIN Retry; Wortzahl `minWords - 20%` (außerhalb Korridor) → Retry getriggert
  </done>
</task>

</tasks>

<verification>
**Static checks (alle drei Tasks):**
```bash
cd web
npx tsc --noEmit
npm run lint
```

**Lokaler Smoke-Test (manuell, einmalig nach allen 3 Tasks):**
1. `cd web && npm run dev`
2. Browser: `/app` → Wizard mit kurzem Input (<200 Zeichen, z. B. "In meiner Straße in Bremen-Neustadt sind die Radwege gefährlich. Bitte ändert das.") durchspielen.
3. Generierter Brief darf KEINE erfundene Vita enthalten (kein "als Mutter von zwei Kindern", "als Lehrer seit 15 Jahren", "in der Neustadt aufgewachsen", wenn das nicht im Input stand). Stadt/Stadtteil aus dem Input ist OK.
4. Strg+F im Brieftext: "Ich verstehe, dass" darf NICHT vorkommen.
5. 3 Test-Briefe mit unterschiedlichen Themen generieren, Konsolen-Logs (`[generateLetter]`) checken: kein `retried: true`-Log, solange Wortzahl im ±15%-Korridor liegt.
6. `/feedback?token=…` (Magic-Link aus einer Test-Bestellung) öffnen, 2 Sterne wählen → die drei neuen Chips "Details über mich erfunden", "Anliegen falsch verstanden", "Wiederholt sich" sind sichtbar.
7. Test-Bewertung mit den neuen Chips absenden → Supabase Studio: `reviews.feedback_tags` enthält die neuen Slugs.

**Telemetrie nach Deploy (folge-up, kein blocker für merge):**
- Vercel-Logs auf `[generateLetter]` filtern: `retried: true` sollte deutlich seltener werden
- Nach ~5-10 echten Briefen: `wordCountInRange`-Quote in `reviews.debug_payload` checken (Ziel-Treffer-Quote sollte sich NICHT verschlechtern, sonst war der Korridor die falsche Stellschraube)
- Nach ~10-20 echten Reviews: Verteilung der neuen Chips analysieren
</verification>

<success_criteria>
1. `npx tsc --noEmit` + `npm run lint` grün
2. `feedbackTags.ts`: 9 Negativ-Chips, 4 Positiv-Chips
3. `generateLetter.ts`: "ÜBER DEN ABSENDER"-Block genau 1× vorhanden, "KOMPLEXITÄT ANERKENNEN" 0× vorhanden, PFLICHT-ELEMENTE hat 3 Punkte (1. ANLASS, 2. BITTE, 3. SCHLUSS), `acceptableMin/Max` berechnet und im Retry-Branch genutzt, `wordCountInRange` UNVERÄNDERT am Ziel-Fenster
4. Smoke-Test im Wizard mit kurzem Input erzeugt keinen Brief mit erfundener Vita und keine "Ich verstehe, dass…"-Floskel
5. Neue Chips erscheinen im Feedback-Formular und landen korrekt in Supabase `reviews.feedback_tags`
</success_criteria>

<output>
Nach Abschluss: `.planning/quick/260521-tn1-review-lernschleife-chips-anti-halluzina/260521-tn1-SUMMARY.md` erstellen mit:
- Geänderte Dateien (2)
- Anzahl Chips vorher/nachher (6/9 negativ)
- Prompt-Änderungen kurz (3: ÜBER DEN ABSENDER, hinweis-Block, Pflicht-Element entfernt)
- Retry-Schwelle vorher/nachher (Min/Max → Min*0.85/Max*1.15)
- Erwartete Wirkung (Latenz -10s in ~50% der Fälle, Halluzinations-Risiko bei kurzem Input gesenkt, Wiederholungs-Bug ersatzlos behoben)
- Telemetrie-Followups (siehe verification section)
</output>
