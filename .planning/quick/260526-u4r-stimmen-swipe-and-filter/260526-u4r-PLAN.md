---
quick_id: 260526-u4r
slug: stimmen-swipe-and-filter
description: /stimmen — touch-swipe für ReviewMarquee, Anzeige nur ab 3 Sternen
date: 2026-05-26
---

# /stimmen: Swipe on touch + filter reviews ≥ 3 stars

## Goal

Auf der `/stimmen` Seite zwei kleine Verbesserungen:

1. **Touch-Swipe statt Auto-Marquee** auf Mobile/iPad. Aktuell läuft die `ReviewMarquee` per CSS-Animation endlos durch — auf Touch-Geräten unergonomisch, kein Pause-on-Hover möglich. Lösung: per `@media (hover: none)` Animation aus, stattdessen natives horizontales Scrollen mit `scroll-snap-type: x mandatory` und Snap-Points pro Karte. Auf Desktop bleibt das bisherige Marquee mit Hover-Pause.

2. **Reviews ≥ 3 Sterne filtern.** Im Marquee und in den Pull-Quotes (Quote 1/Quote 2) nur Reviews mit `rating >= 3` zeigen. Die `RatingStat` und das `AggregateRating` JSON-LD weiter über alle Reviews berechnen — der Durchschnitt soll alles widerspiegeln.

## Files to modify

- `web/src/components/reviews/ReviewMarquee.tsx` — CSS: bei `@media (hover: none)` Animation deaktivieren, `scroll-snap-type` + `scroll-snap-align` aktivieren, `overflow-x-auto` statt `hidden`, Track-Duplikat unterdrücken (sonst doppelte Snap-Points).
- `web/src/app/(site)/stimmen/page.tsx` — gefilterte Review-Liste an `ReviewMarquee` und `pickHighlights` übergeben; `stats` bleibt vom unveränderten Aufruf.

## Approach

### ReviewMarquee.tsx

Bestehendes `<style>` erweitern:

```css
@media (hover: none) {
  .marquee-track {
    animation: none;
  }
  .marquee-container {
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }
  .marquee-container::-webkit-scrollbar { display: none; }
  .marquee-snap-item {
    scroll-snap-align: center;
  }
}
```

`overflow-x-hidden` Class entfernen oder durch Style-Override im Media-Query überschreiben. Track-Verdoppelung (`[...items, ...items]`) auf Touch-Geräten optisch egal — es sind nur mehr Karten zum durchswipen. OK so lassen, kein zusätzlicher JS-Branch nötig.

Snap-Item Klasse auf den Button setzen.

### page.tsx

```ts
const reviewsForDisplay = reviews.filter((r) => r.rating >= 3);
const highlights = pickHighlights(reviewsForDisplay);
```

`<ReviewMarquee reviews={reviewsForDisplay} … />` und `hasReviews` aus der gefilterten Liste ableiten.

`pickHighlights` ändert sich nicht — der `r.rating === 5` Filter darin bleibt strenger.

## Tasks

1. **Update ReviewMarquee CSS for touch swipe**
   - File: `web/src/components/reviews/ReviewMarquee.tsx`
   - Add `@media (hover: none)` block to disable animation, enable `overflow-x: auto` + `scroll-snap-type: x mandatory`
   - Add `marquee-snap-item` class to each card button
   - Verify: keine Regression auf Desktop (Hover-Pause + Auto-Scroll funktioniert weiter)

2. **Filter reviews ≥ 3 stars in stimmen page**
   - File: `web/src/app/(site)/stimmen/page.tsx`
   - Derive `reviewsForDisplay = reviews.filter(r => r.rating >= 3)`
   - Pass to ReviewMarquee and pickHighlights; keep `stats` from full set
   - Verify: `RatingStat` Durchschnitt unverändert; nur ≥3-Sterne im Marquee sichtbar

## Must-haves

- Marquee ist auf Touch-Geräten frei swipebar (CSS-Snap, kein Auto-Loop)
- Desktop-Verhalten unverändert (hover pauses, click expands, esc closes)
- Im Marquee sichtbare Reviews haben alle `rating >= 3`
- `RatingStat` und `AggregateRating` JSON-LD basieren weiter auf allen Reviews
- Build läuft (`npm run build` in `web/`), keine TypeScript-Fehler
