---
phase: quick-260527-wu1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(site)/was-bisher-geschah/page.tsx
  - web/src/components/Footer.tsx
  - web/src/app/sitemap.ts
autonomous: true
requirements: [WU1-LOG-PAGE, WU1-NAV-WIRING]
must_haves:
  truths:
    - "Besucher erreichen /was-bisher-geschah und sehen ein nach Monat gruppiertes Fortschritts-Log, neuester Monat oben"
    - "Oben auf der Seite verlinkt ein deutlicher Verweis nach /was-noch-kommt (Roadmap)"
    - "Die Seite folgt 1:1 dem visuellen Stil von /was-noch-kommt (gleiche Tokens, Fonts, Struktur, JSON-LD)"
    - "Footer enthält einen Link zu /was-bisher-geschah"
    - "Sitemap enthält /was-bisher-geschah"
    - "Projekt baut sauber (npm run build erfolgreich)"
  artifacts:
    - path: "web/src/app/(site)/was-bisher-geschah/page.tsx"
      provides: "Statische Changelog-Seite, monatsgruppiert, newest-first"
      contains: "Was bisher geschah"
    - path: "web/src/components/Footer.tsx"
      provides: "Footer-Link auf die neue Seite"
    - path: "web/src/app/sitemap.ts"
      provides: "Sitemap-Eintrag der neuen Seite"
  key_links:
    - from: "web/src/app/(site)/was-bisher-geschah/page.tsx"
      to: "/was-noch-kommt"
      via: "next/link near top of page"
      pattern: "was-noch-kommt"
    - from: "web/src/components/Footer.tsx"
      to: "/was-bisher-geschah"
      via: "next/link"
      pattern: "was-bisher-geschah"
---

<objective>
Neue statische Seite /was-bisher-geschah ("Was bisher geschah"): ein kuratiertes, nach Monat gruppiertes Fortschritts-Log (Changelog), neuester Monat oben. Es baut Vertrauen auf: Besucher sehen stetigen, echten Fortschritt, was die Bewertungen auf /stimmen glaubwürdiger macht. Oben ein deutlicher Verweis nach oben zur Roadmap (/was-noch-kommt), sodass beide Seiten einander rahmen.

Purpose: Vertrauen durch sichtbaren Fortschritt; Querverweis Log <-> Roadmap.
Output: 1 neue Seite + Footer-Link + Sitemap-Eintrag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# DESIGN-VORLAGE — die neue Seite folgt diesem Stil 1:1.
@web/src/app/(site)/was-noch-kommt/page.tsx
@web/src/components/Footer.tsx
@web/src/app/sitemap.ts
@web/src/lib/config.ts
@web/src/components/editorial/Prose.tsx
@web/src/components/FAQAccordion.tsx

# WICHTIG: Dieses Repo nutzt ein modifiziertes Next.js (App Router, (site) route group).
@web/AGENTS.md

<interfaces>
<!-- Aus dem Codebase extrahiert. Direkt verwenden, keine Erkundung nötig. -->

Aus web/src/lib/config.ts:
```typescript
export const APP_URL = "https://www.brief-nach-berlin.de";
// FOUNDER_FEEDBACK_URL existiert, wird hier aber NICHT benötigt.
```

Vorlage-Muster aus was-noch-kommt/page.tsx (1:1 übernehmen):
- Wrapper: `<div className="min-h-screen bg-creme px-6 py-20">` -> innen `<div className="max-w-2xl mx-auto">`
- Back-Link: `<Link href="/" className="font-typewriter text-sm text-waldgruen hover:text-waldgruen-dark transition-colors mb-8 inline-block">&larr; Zurück</Link>`
- Eyebrow: `<p className="font-typewriter text-sm font-bold tracking-widest uppercase text-waldgruen/60 mb-3">Fortschritt</p>`
- H1: `<h1 className="font-body text-3xl md:text-5xl font-bold text-waldgruen-dark tracking-tight mb-6 text-balance">Was bisher geschah</h1>`
- Handwriting-Subhead: `<p className="font-handwriting text-xl md:text-2xl text-warmgrau leading-relaxed mb-10 text-pretty">...</p>`
- Card: `bg-white border border-waldgruen/15 rounded-2xl p-6 sm:p-8 shadow-sm`
- Monats-Badge (analog status-Badge): `inline-block rounded-full px-3 py-1 font-typewriter text-xs font-bold uppercase tracking-widest bg-waldgruen text-creme`
- Final CTA: Box `mt-16 p-8 border-2 border-waldgruen/20 bg-creme/50 rounded-sm` mit `<Link href="/">Brief schreiben &rarr;</Link>` (waldgruen-dark Button)
- JSON-LD Article: identischer Block, nur headline/description/URL_PATH angepasst.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seite /was-bisher-geschah erstellen</name>
  <files>web/src/app/(site)/was-bisher-geschah/page.tsx</files>
  <action>
Erstelle die neue Seite analog zu was-noch-kommt/page.tsx. Struktur in dieser Reihenfolge:

1. Imports: `Link` (next/link), `Metadata` (next), `ReactNode` (react), `APP_URL` aus "@/lib/config". KEIN Image (kein Hero-Bild nötig). KEINE neuen Dependencies.

2. Konstanten: `URL_PATH = "/was-bisher-geschah"`, `PUBLISHED = "2026-05-27"`, `TITLE = "Was bisher geschah: Der Fortschritt von Brief nach Berlin"`, `DESCRIPTION` (1 Satz, ehrlich, ich-Form, beschreibt das offene Fortschritts-Log).

3. `metadata: Metadata` exakt nach Vorlage (title, description, alternates.canonical, openGraph type "article", twitter summary_large_image).

4. Typed Daten-Array nach dem `levels`-Muster:
```typescript
interface Monat {
  key: string;        // "mai-2026"
  badge: string;      // "Mai 2026"
  entries: string[];  // gebündelte Punkte
}
const monate: Monat[] = [ ... ];
```
Befülle mit dem content_seed aus dem Quick-Intent, NEWEST FIRST (Mai 2026, dann April 2026, dann März 2026). Verfeinere die deutsche Prosa leicht, aber behalte Substanz und Reihenfolge. Voice-Regeln strikt: nur Deutsch, KEINE Em-Dashes (nur Komma/Doppelpunkt/Punkt/Bindestrich), durchgehend Ich-Form (kein "wir"), keine KI-Floskeln (kein "nahtlos", keine Dreier-Adjektivketten, kein "Fazit"), konkret statt werblich. "Brief-nach-Berlin" mit Bindestrichen, wenn als Name im Satz.

5. `articleJsonLd` Block exakt nach Vorlage (headline = TITLE ohne Marken-Suffix, description, datePublished/dateModified = PUBLISHED, author/publisher Organization "Brief nach Berlin", mainEntityOfPage, inLanguage "de-DE"). Als `<script type="application/ld+json">` rendern. KEIN FAQ-JSON-LD nötig (keine FAQ auf dieser Seite).

6. Render-Body (im `max-w-2xl`-Container):
   - Back-Link "&larr; Zurück" nach "/".
   - Eyebrow "Fortschritt", H1 "Was bisher geschah", Handwriting-Subhead (ehrlich, ich-Form, z.B. dass ich offen baue und hier zeige, was schon passiert ist).
   - DEUTLICHER Verweis nach oben zur Roadmap, direkt unter der Subhead, vor den Monaten. Nutze einen umrahmten Link-Block (Muster wie GEO-Answer-Block `border-l-4 border-waldgruen pl-6 py-2` oder eine weiße Card mit hover-border), Text sinngemäß: "Das hier ist, was schon passiert ist. Was als Nächstes kommt, steht in der Roadmap." mit `<Link href="/was-noch-kommt">Was noch kommt &rarr;</Link>` (waldgruen-dark, underline oder Button-Stil).
   - Monats-Sektionen: `monate.map(...)` -> je eine `<section>` Card (`bg-white border border-waldgruen/15 rounded-2xl p-6 sm:p-8 shadow-sm`). Kopf: Monats-Badge (Pill, `bg-waldgruen text-creme`). Darunter eine `<ul>` mit den `entries` als Listenpunkte (`font-body text-base text-warmgrau leading-relaxed`, dezente Bullets, `space-y-2`). Container der Sektionen: `space-y-6 mb-16`.
   - Final CTA-Box exakt nach Vorlage: Eyebrow, kurzer ich-Form-Text, `<Link href="/">Brief schreiben &rarr;</Link>` Button (waldgruen-dark).

Halte dich strikt an die bestehenden Farb-Tokens (waldgruen/waldgruen-dark/creme/warmgrau/airmail) und Fonts (font-typewriter/font-handwriting/font-body). Keine neuen Designentscheidungen, keine neuen Komponenten.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run lint 2>/dev/null || npm run build</automated>
  </verify>
  <done>Seite existiert unter web/src/app/(site)/was-bisher-geschah/page.tsx, exportiert metadata + default-Komponente, rendert 3 Monatssektionen newest-first, enthält Roadmap-Verweis oben und Final-CTA, baut/typecheckt sauber.</done>
</task>

<task type="auto">
  <name>Task 2: Footer-Link und Sitemap-Eintrag ergänzen</name>
  <files>web/src/components/Footer.tsx, web/src/app/sitemap.ts</files>
  <action>
1. In web/src/components/Footer.tsx: Füge in der "Mehr lesen"-Linkliste einen neuen `<Link href="/was-bisher-geschah" className="font-body text-sm text-warmgrau/70 hover:text-waldgruen-dark transition-colors duration-200">Was bisher geschah: Fortschritt</Link>` ein. Platziere ihn direkt VOR dem bestehenden "/was-noch-kommt"-Link (Zeile ~81-86), damit Log und Roadmap nebeneinander stehen. Übernimm exakt die className der anderen Links.

2. In web/src/app/sitemap.ts: Füge einen neuen Eintrag ins zurückgegebene Array ein:
```typescript
{
  url: `${BASE_URL}/was-bisher-geschah`,
  lastModified,
  changeFrequency: "weekly",
  priority: 0.6,
},
```
Platziere ihn sinnvoll bei den verwandten Einträgen (z.B. direkt nach dem "/was-noch-kommt"-Eintrag).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Footer enthält Link zu /was-bisher-geschah vor dem Roadmap-Link; sitemap.ts enthält Eintrag mit changeFrequency "weekly" und priority 0.6; Build erfolgreich.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` läuft fehlerfrei durch.
- Manuell (optional): /was-bisher-geschah rendert, Mai oben, März unten, Roadmap-Link oben sichtbar, Footer-Link vorhanden.
- Voice-Check: keine Em-Dashes, kein "wir", keine KI-Floskeln im Seitentext.
</verification>

<success_criteria>
- Neue Seite /was-bisher-geschah live, monatsgruppiert newest-first, im Stil von was-noch-kommt.
- Roadmap-Verweis oben, Final-CTA unten.
- Footer- und Sitemap-Eintrag vorhanden.
- Build sauber, keine neuen Dependencies, keine neuen Komponenten.
</success_criteria>

<output>
After completion, create `.planning/quick/260527-wu1-subpage-was-bisher-geschah-kuratiertes-f/260527-wu1-SUMMARY.md`
</output>
