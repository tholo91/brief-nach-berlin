---
phase: quick-260626-h4r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/Hero.tsx
  - web/src/components/wizard/Step2Issue.tsx
  - web/src/components/HowItWorksWithExample.tsx
autonomous: false
requirements: [QUICK-260626-h4r]
must_haves:
  truths:
    - "Im Hero steht ueber dem Anliegen-Feld nichts mehr: die idle-Tagline UND der bei Tippbeginn erscheinende Tips-Crossfade ('Je konkreter...') sind entfernt. H1 geht direkt ins Feld."
    - "Der Pill-Link 'BALD: Land & Kommune' ist aus dem Hero entfernt (wird weiter unten auf der Landing ohnehin referenziert)."
    - "Die 3 verbleibenden Trust-Badges (kostenlos / ohne Account / KI aus EU) laufen in JEDEM Viewport ab ca. 360px in EINER Zeile, kein Umbruch."
    - "Das Landing-Anliegenfeld startet 2 Zeilen hoch statt 1 und waechst danach wie bisher weiter."
    - "Die Landing-Placeholder sind laenger (fuellen ~2 Zeilen, laden zum Mehr-Schreiben ein) und werden auf schmalen Screens mit '…' auf max. 2 Zeilen gekappt, nie auf eine 3. Zeile."
    - "Das Argument 'Du musst kein Politik-Profi sein. Das Tool findet die richtigen Worte und Adressen.' erscheint jetzt als Unterzeile in 'So einfach geht's' (Schritt 2), nicht mehr im Hero."
    - "Auf Mobile und iPad bleibt die Press-/Review-Marquee im ersten Viewport sichtbar: der Hero wird netto NICHT hoeher als vorher."
    - "Keine neue npm-Dependency."
  artifacts:
    - path: "web/src/components/Hero.tsx"
      provides: "Verschlankter Hero: ohne Tagline/Tips-Block, ohne BALD-Link, ohne writing/charCount/tipsOpened-State"
      contains: "Step2Issue"
    - path: "web/src/components/wizard/Step2Issue.tsx"
      provides: "Landing-Feld startet 2-zeilig; laengere Landing-Placeholder mit 2-Zeilen-Kappung"
      contains: "LANDING_PLACEHOLDER"
    - path: "web/src/components/HowItWorksWithExample.tsx"
      provides: "Reassurance-Unterzeile unter der H2 'So einfach geht's'"
      contains: "Politik-Profi"
  key_links:
    - from: "web/src/components/Hero.tsx"
      to: "web/src/components/wizard/Step2Issue.tsx (variant=landing)"
      via: "Step2Issue rendert das Landing-Feld; Hoehe/Placeholder dort geregelt"
      pattern: "variant=\"landing\""
---

<objective>
Den Hero von Brief-nach-Berlin entschlacken, damit Erstbesucher nicht von Tipps und Zusatzinfos ueberfordert werden, und das Anliegen-Feld einladender machen (2-zeilig + laengere Beispiele), ohne die fuer Credibility wichtige Marquee aus dem ersten Viewport zu draengen.

Purpose: Klarerer, fokussierter Einstieg (H1 -> Feld), staerkeres "Schreib ruhig mehr"-Signal, weniger visuelles Rauschen ueber dem Feld.
Output: 3 chirurgische Aenderungen in 3 Dateien, kein Refactoring umliegenden Codes, keine neue Dependency.

Erledigt im UX-Vorgespraech (Entscheidungen Thomas):
- Tagline UND char-1-Tips-Crossfade raus aus dem Hero.
- 4 -> 3 Badges: "BALD: Land & Kommune" raus.
- Feld 2-zeilig; Placeholder laenger, auf Mobile mit "…" gekappt.
- Tagline-Argument zieht in "So einfach geht's" um (eigener, abtrennbarer Schritt).
- KEINE statische Hint-Zeile unter dem Feld (dort ist mit Voice/Char-Counter/Badges schon genug los).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
Frontend-Umsetzung laeuft ueber /frontend-design (Repo-Konvention fuer Frontend-Edits).
</execution_context>

<context>
@web/AGENTS.md

<verified_facts>
Gegen den echten Code verifiziert (nicht aus Trainingsdaten angenommen):

- Hero (web/src/components/Hero.tsx):
  - Der Block ueber dem Feld ist `<div className="relative mb-2 min-h-[3.75rem] ...">` (Zeilen ~113-130): er reserviert ~60px und enthaelt die idle-Tagline `TAGLINE_IDLE` (Zeile 15-16) sowie den `<TipsDisclosure variant="landing" ...>` -Crossfade.
  - Gesteuert wird der Crossfade ueber `charCount`/`writing` (Zeile 21-25, `HERO_EXPAND_CHARS = 1`) und `tipsOpened`/`setTipsOpened` (Zeile 24, 41).
  - `handleSubmit` (Zeile 33-45) ruft `saveHandoff({ issueText, tipsOpened, usedSpeechToText })`. `tipsOpened` ist in WizardHandoff OPTIONAL (web/src/lib/wizard-handoff.ts:17) -> kann ersatzlos weggelassen werden, KEIN Typ-Bruch.
  - Der BALD-Pill ist der `<Link href="/was-noch-kommt" ...>` -Block (Zeilen ~171-187) im selben flex-Container wie die 3 Badges.
  - Trust-Badges (Zeilen ~149-170): 3 `<span>` mit kurzen Mobile-Labels (`kostenlos` / `ohne Account` / `KI aus EU`) via `sm:hidden`/`hidden sm:inline`. Container: `flex flex-wrap items-center justify-center gap-x-2 gap-y-1`.

- Marquee-Mechanik (web/src/app/page.tsx:32-48):
  - Hero ist `min-h-[72vh] lg:min-h-[92vh]` mit `items-start lg:items-center`. Die Marquee-Section zieht sich per `-mt-20 md:-mt-24 lg:-mt-24` in den unteren Hero-Bereich.
  - Auf Mobile/iPad haengt die Marquee-Position am 72vh-Rahmen, nicht an der exakten Content-Hoehe -> solange der Content NICHT ueber 72vh laeuft, bleibt sie sichtbar. Das Entfernen des ~60px-Blocks gewinnt mehr Platz, als die 2. Feldzeile (~28px) kostet -> Hero netto kuerzer. MUSS im Verify bei 375px + iPad geprueft werden.

- Step2Issue Landing-Feld (web/src/components/wizard/Step2Issue.tsx):
  - Landing hat KEINE feste Min-Hoehe: startet 1 Zeile (`fieldHeight` initial 52, Zeile 279) und waechst. Wizard nutzt `HEIGHT_MIN_WIZARD = 160` (Zeile 239). `HEIGHT_MAX = 320` (Zeile 238).
  - `micCentered` (Zeile 280, initial `isLanding`) zentriert das Mic vertikal, solange einzeilig; kippt beim Umbruch in die 2. Zeile nach oben-rechts.
  - Placeholder-Sets: `LANDING_PLACEHOLDER_EXAMPLES` (kurz, schmale Phones, Zeile 185-193) und `LANDING_PLACEHOLDER_EXAMPLES_WIDE` (iPad/Desktop, Zeile 199-207). Auswahl ueber `isWide` (>=640px, Zeile 286, 303-307).
  - 1-Zeilen-Kappung: `displayPlaceholder = ellipsize(placeholder, phWidth, phFont)` (Zeile 314-315). `ellipsize()` (Zeile 217-231) misst per Canvas gegen EINE Zeilenbreite (`phWidth`) und haengt "…" an. Textarea-Placeholder ignorieren `text-overflow: ellipsis` -> JS-Kappung ist noetig.
  - `onCharCountChange` (Prop, Zeile 255) wird NUR vom Hero genutzt. Wird der Hero-Block entfernt, kann der Prop bleiben (ungenutzt) ODER der `onCharCountChange`-Call im Hero faellt einfach weg.

- "So einfach geht's" (web/src/components/HowItWorksWithExample.tsx):
  - Header-Block Zeile 82-89: Eyebrow "In drei Schritten" + H2 "So einfach geht's". Direkt darunter ist die natuerliche Stelle fuer die Reassurance-Unterzeile. Schritte 01/02 ("Erzaehl, was dich beschaeftigt" / "findet die passende Adresse") spiegeln "richtige Worte und Adressen" bereits -> inhaltlich anschlussfaehig.
</verified_facts>

<copy_guardrails>
- Keine Em-Dashes in User-facing Copy (Bindestriche/Kommas/Doppelpunkte). Siehe Memory.
- Neue/umgezogene Copy gegen signs-of-ai-writing.md pruefen. Die umziehende Zeile ist bestehende, menschlich geschriebene Copy -> moeglichst woertlich uebernehmen.
</copy_guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero entschlacken (Tagline/Tips raus, BALD raus, State aufraeumen)</name>
  <files>web/src/components/Hero.tsx</files>
  <action>
Chirurgisch, KEIN Refactoring der restlichen Komponente:

1. Den gesamten Block ueber dem Feld entfernen: das `<div className="relative mb-2 min-h-[3.75rem] ...">` mit der idle-Tagline `<p>{TAGLINE_IDLE}</p>` UND dem `<TipsDisclosure variant="landing" ...>`-Crossfade (aktuell ~Zeile 107-130). Ueber dem Feld bleibt nichts -> die H1 grenzt direkt an den `#anliegen`-Wrapper.
2. Den BALD-Pill `<Link href="/was-noch-kommt" ...>` (aktuell ~Zeile 171-187) ersatzlos entfernen. Die 3 Trust-Badges bleiben.
3. Toten State/Konstanten aufraeumen, die nur den Crossfade trieben: `charCount`/`setCharCount`, `writing`, `tipsOpened`/`setTipsOpened`, `HERO_EXPAND_CHARS`, `TAGLINE_IDLE`. Aus `handleSubmit` das `tipsOpened` aus `saveHandoff(...)` entfernen (Prop ist optional, kein Typ-Bruch) und aus der `useCallback`-Deps-Liste streichen. Am `<Step2Issue ... />` den `onCharCountChange={setCharCount}` entfernen.
4. Sicherstellen, dass die 3 verbleibenden Badges in ALLEN Viewports (>=360px) in EINER Zeile laufen. Erst mit dem bestehenden `flex flex-wrap` im Preview pruefen (ohne den breiten BALD-Pill passen die 3 kurzen Mobile-Labels sehr wahrscheinlich). NUR falls sie doch umbrechen: auf `flex-nowrap` wechseln und `gap-x` minimal reduzieren, ohne dass Labels abgeschnitten werden.

Imports pruefen: wird `TipsDisclosure` (Import aus Step2Issue) sonst nirgends im Hero genutzt, den Import entfernen. `Link` wird nach Entfernen des BALD-Pills evtl. nicht mehr gebraucht -> dann Import entfernen.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `npx tsc --noEmit` ohne neue Fehler; keine ungenutzten Imports/Variablen (ESLint sauber).
- Im Preview: ueber dem Feld steht nichts mehr, kein BALD-Pill, 3 Badges in einer Zeile bei 360/375px und iPad.
  </done>
</task>

<task type="auto">
  <name>Task 2: Landing-Feld 2-zeilig + laengere Placeholder mit 2-Zeilen-Kappung</name>
  <files>web/src/components/wizard/Step2Issue.tsx</files>
  <action>
Nur den Landing-Pfad (`isLanding`) anfassen, Wizard-Verhalten unveraendert lassen.

WICHTIG - VT-Morph schuetzen: Der `<FieldMorph>`/`<ViewTransition name="anliegen-feld">`-Wrapper (Step2Issue.tsx ~Zeile 13-24 + 496) und das `<div className="relative">` darin sind der geteilte Morph Landing<->Wizard (router.push("/app")). Diesen Wrapper, den `name="anliegen-feld"` und die globals.css `::view-transition-*(anliegen-feld)`-Regeln NICHT anfassen. Hoehen-/Placeholder-Aenderungen wirken nur INNERHALB des Felds und lassen den Morph intakt (2-zeiliger Start liegt sogar naeher an der Wizard-Groesse -> ruhigerer Morph). Beim Verify gegenpruefen, dass der Morph in Chrome/Edge weiterhin laeuft.

1. Starthoehe 2 Zeilen: eine Landing-Min-Hoehe einfuehren (analog `HEIGHT_MIN_WIZARD`), z.B. `HEIGHT_MIN_LANDING` ~80px (2 Zeilen bei der Landing-Schriftgroesse). In der `resize()`-Clamp-Logik fuer `isLanding` diese Min statt der heutigen 1-Zeilen-Hoehe verwenden; `fieldHeight`-Initialwert entsprechend anheben, damit es ohne Flicker direkt 2-zeilig rendert. `HEIGHT_MAX = 320` bleibt; danach scrollt das Feld wie bisher.
2. Mic-Position: mit leerem 2-Zeilen-Feld soll das Mic sinnvoll sitzen (vertikal mittig auf dem 2-Zeilen-Block ODER oben-rechts). Bestehende `micCentered`-Logik im Preview pruefen und nur minimal anpassen, falls das Mic schief wirkt. Keine Neuverdrahtung.
3. Placeholder laenger + 2-Zeilen-Kappung:
   - Die Landing-Placeholder-Saetze auf "fuellt ~2 Zeilen"-Laenge bringen (laenger als `LANDING_PLACEHOLDER_EXAMPLES_WIDE`, kuerzer als die langen Wizard-Beispiele). Gleiche Themen (Radwege, Klima, Mieten, Kinderarzt, Schule/Tablets, Waldrodung, Ton in der Politik). Saetze als vollwertige Mini-Beispiele formulieren, die zum Mehr-Schreiben einladen.
   - Kappung auf max. 2 Zeilen: `ellipsize()` so nutzen, dass gegen ein 2-Zeilen-Budget gemessen wird (z.B. `phWidth * 2` statt `phWidth`), damit der Placeholder nie auf eine 3. Zeile umbricht und bei Ueberlaenge mit "…" endet. Auf breiten Screens (`isWide`), wo die Saetze in 2 Zeilen passen, bleibt "…" aus.
   - Pruefen, ob auf schmalen Phones (`!isWide`) ein eigenes, etwas kuerzeres 2-Zeilen-Set noetig ist, damit nicht staendig hart gekappt wird. Falls ja: das schmale Set entsprechend formulieren (gleiche Themen).
   - Voice-Placeholder ("Sprich jetzt..."/"Transkribiere...") unveraendert lassen.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `npx tsc --noEmit` ohne neue Fehler.
- Im Preview (375px, iPad, Desktop): Feld startet 2-zeilig; Placeholder fuellt ~2 Zeilen; auf schmalem Phone sauber mit "…" gekappt, nie 3 Zeilen; Wizard-Feld unveraendert.
  </done>
</task>

<task type="auto">
  <name>Task 3 (abtrennbar): Reassurance-Unterzeile in "So einfach geht's"</name>
  <files>web/src/components/HowItWorksWithExample.tsx</files>
  <action>
Unter der H2 "So einfach geht's" (Header-Block ~Zeile 82-89) eine ruhige Unterzeile ergaenzen, die das aus dem Hero entfernte Argument aufnimmt: "Du musst kein Politik-Profi sein. Das Tool findet die richtigen Worte und Adressen." (bestehende, menschlich geschriebene Copy moeglichst woertlich; keine Em-Dashes).
Styling am bestehenden Header-Stil orientieren (z.B. `font-body text-warmgrau`-Subline, zentriert, mit `mt`-Abstand und sinnvoller `max-w`). Reine Ergaenzung, an Schritten/Beispielkarte nichts aendern.
Dieser Task ist von Task 1+2 unabhaengig und kann separat geshipt/zurueckgestellt werden.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `npx tsc --noEmit` ohne neue Fehler.
- Im Preview: Unterzeile sitzt sauber unter der H2, lesbar auf Mobile + Desktop.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Verschlankter Hero (Tagline/Tips + BALD raus), 2-zeiliges Landing-Feld mit laengeren Placeholdern, Reassurance-Unterzeile in "So einfach geht's".</what-built>
  <how-to-verify>
1. `cd web && npm run dev`, http://localhost:3000 oeffnen.
2. Desktop: Ueber dem Feld steht nichts mehr; kein "BALD"-Pill; 3 Badges in einer Zeile; Feld ist 2-zeilig; Placeholder rotiert und fuellt ~2 Zeilen.
3. Mobile (375px) + iPad-Breite: 3 Badges weiterhin in EINER Zeile; Placeholder auf schmalem Screen sauber mit "…" gekappt (nie 3 Zeilen); Press-/Review-Marquee weiterhin im ersten Viewport sichtbar (Hero nicht hoeher als vorher).
4. "So einfach geht's": Unterzeile mit dem "kein Politik-Profi"-Argument sichtbar.
5. Tippen + Submit aus dem Hero funktioniert weiter (Text kommt im Wizard vorausgefuellt an).
6. VT-Morph intakt (Chrome/Edge): Feld morpht beim Submit weich Landing -> Wizard, kein harter Schnitt.
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe, was nicht passt (z.B. Badge-Umbruch, Placeholder-Kappung, Marquee aus dem Viewport).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sauber; keine ungenutzten Imports/Variablen.
- Nur die 3 gelisteten Dateien angefasst, kein Refactoring umliegenden Codes.
- Keine neue Dependency.
- Marquee bei 375px + iPad im ersten Viewport sichtbar; 3 Badges einzeilig; Placeholder nie >2 Zeilen.
</verification>

<success_criteria>
- Hero ist ruhiger: H1 -> Feld, nichts dazwischen; kein BALD-Pill; 3 Badges einzeilig.
- Landing-Feld 2-zeilig, laedt sichtbar zum Mehr-Schreiben ein; Placeholder laenger, auf Mobile gekappt.
- "kein Politik-Profi"-Argument lebt jetzt in "So einfach geht's".
- Marquee bleibt fuer Credibility im ersten Viewport.
</success_criteria>

<output>
Nach Abschluss: `.planning/quick/260626-h4r-hero-vereinfachen-2zeiliger-input/260626-h4r-SUMMARY.md` erstellen.
</output>
