# Quick Task 260628-ta0: Marken-Entity-Schema Organization und WebSite JSON-LD im Next Root-Layout ergänzen

**Date:** 2026-06-28
**Status:** Ready for execution

## Goal

Add one server-rendered brand JSON-LD block to the Next.js root layout so the homepage and all routes expose a stable `Organization` + `WebSite` entity graph.

## Scope

- Edit `web/src/app/layout.tsx` only for code.
- Keep all existing metadata, sitemap, robots, Open Graph, Twitter, Article, and FAQ schemas unchanged.
- Use `APP_URL`, `FOUNDER_HOMEPAGE`, and `FOUNDER_LINKEDIN` from `@/lib/config`.
- Use `sameAs` conservatively:
  - `Organization.sameAs`: official project repository.
  - `Person.sameAs`: founder website and LinkedIn.

## Tasks

1. Add `brandJsonLd` in `web/src/app/layout.tsx`
   - Include `@context`, `@graph`, stable `@id` values, `Organization`, founder `Person`, and `WebSite`.
   - Reference `APP_URL`, `/apple-icon.png`, and `/opengraph-image.jpg`.

2. Render JSON-LD in the root body
   - Add one `<script type="application/ld+json">` before `{children}`.
   - Use `JSON.stringify(brandJsonLd)`.

3. Verify
   - Run TypeScript/build checks available in `web/package.json`.
   - Curl a locally served homepage and confirm the HTML includes the JSON-LD graph.
   - Parse the emitted script JSON locally to confirm valid JSON and expected schema nodes.

## Done

- Homepage HTML contains `Organization`, `Person`, and `WebSite` JSON-LD.
- `Organization.@id` and `WebSite.@id` are stable.
- Existing metadata and page-level JSON-LD are untouched.
